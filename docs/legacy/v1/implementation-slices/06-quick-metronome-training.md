# Pack 6 Slice Backlog: Quick Metronome Training

## Pack Goal

Quick Metronome becomes a richer local training tool while keeping fast-start practice intact.

## Slice Backlog

### P6-01 `auto-increase-domain`
- Define tempo ramp settings and schedule.

### P6-02 `auto-increase-scheduler`
- Apply ramp changes to generated metronome clicks.

### P6-03 `auto-increase-ui`
- Add compact Auto Increase controls and state.

### P6-04 `mute-training-domain`
- Define audible/silent measure pattern and cycle state.

### P6-05 `mute-training-scheduler`
- Produce intentional silence and audible clicks with timing evidence.

### P6-06 `mute-training-ui`
- Add compact pattern controls and silent-state display.

### P6-07 `quick-templates-repository`
- Save/load/rename/delete local quick templates.

### P6-08 `quick-templates-ui`
- Add template controls to Quick Metronome.

### P6-09 `warmup-routines-domain-repository`
- Store local warmup routines and steps.

### P6-10 `warmup-routines-runner`
- Run routine steps sequentially through metronome service.

### P6-11 `warmup-routines-ui`
- Add compact routine editor/runner UI.

### P6-12 `tempo-progress-history-source`
- Derive local tempo practice history.

### P6-13 `tempo-progress-history-ui`
- Show honest tempo history summaries.

### P6-14 `quick-advanced-countdown`
- Add beats/measures countdown if not completed in Pack 4.

## Scheduling Notes

Scheduler/timing slices need timing evidence and likely Tier D assignment.

