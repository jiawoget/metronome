---
phase: 01-canonical-practice-presentation-formatting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/home-dashboard.test.tsx
  - tests/unit/session-comparison.test.ts
  - tests/unit/architecture-boundaries.test.ts
  - src/domain/practice/format.ts
  - src/domain/practice/session-comparison.ts
  - src/hooks/use-practice-session-dashboard.ts
  - src/components/home/home-dashboard.tsx
  - src/components/home/practice-goals-panel.tsx
  - src/components/home/practice-goal-editor.tsx
autonomous: true
requirements: [FMT-01, FMT-02, EVID-01, SLIM-01, QUAL-01, HEALTH-01, DELIV-01]
must_haves:
  truths:
    - '[edge:FMT-01/boundary] Timestamp and minute-duration outputs match the research matrix at every threshold and adjacent value: 1, 59_999, 60_000, 119_999, 3_599_999, 3_600_000, and 3_660_000.'
    - '[edge:FMT-01/empty] General null, empty, and invalid timestamps render Unknown time; non-empty invalid analytics renders Unknown update time; whitespace-only analytics suppresses the updated suffix.'
    - '[edge:FMT-01/encoding] Valid timestamps use fixed ASCII YYYY-MM-DD HH:mm UTC output without locale-sensitive text or raw invalid input.'
    - '[edge:FMT-01/precision] UTC fields omit seconds/milliseconds; minute durations floor, exact hours omit zero minutes, and larger finite values have no cap/day conversion.'
    - '[edge:FMT-01/idempotency] Both canonical formatters return the same string for repeated equal inputs without mutation or shared state.'
    - '[edge:FMT-01/concurrency] Concurrent calls are independent pure computations with no I/O, timers, locale state, or shared mutable state.'
    - '[edge:FMT-02/boundary] Seven timestamp and three minute-duration calls use the canonical owner and all seven retired bodies/wrappers are absent at reviewedProductionSha.'
    - '[edge:FMT-02/adjacency] Selected callers share the practice owner while seconds-scale, M:SS, and sheet-library formatters remain separate and unchanged.'
    - '[edge:FMT-02/empty] No legacy selected formatter body, compatibility name, facade, adapter, feature flag, manifest change, or alternate formatter owner remains; the only new production paths are the two admitted CAP-03 Goals components.'
    - '[edge:FMT-02/ordering] Characterization and restoration precede one atomic production consolidation; no accepted product revision is partially migrated.'
    - '[edge:FMT-02/precision] The formatter change adds only the exact CAP-01/CAP-02 exports; CAP-03 adds only the two named cohesive Goals components and no generic abstraction.'
    - '[edge:EVID-01/concurrency] Selection evidence is bound to immutable source/tree/blob and semantic-index identities; drift refreshes before production edits.'
    - '[edge:SLIM-01/concurrency] LOC compares immutable productionBaseSha and reviewedProductionSha only after clean source and formatter-input checks.'
    - '[edge:QUAL-01/boundary] Public characterization covers every research-specified timestamp, fallback, finite-value, threshold, and hour/minute boundary.'
    - '[edge:QUAL-01/adjacency] Tests include adjacent thresholds and separately pin excluded 0s, <1s, and 1m 5s outputs.'
    - '[edge:QUAL-01/empty] Legitimate seams cover null, empty, whitespace, invalid, NaN, infinities, negative, and zero without unsafe casts.'
    - '[edge:QUAL-01/ordering] Green characterization, mutation-red/restoration, production edits, full product gates, disposable rollback, and readiness to enter native verification/validation/security occur in that order.'
    - '[edge:QUAL-01/precision] Required commands run without watch, skip, suppression, or hook bypass; managed-environment failures are never classified as product outcomes.'
    - '[edge:HEALTH-01/boundary] Every existing changed source has no Code Health decline/new severe finding, both new Goals sources have no severe finding, and home-dashboard.tsx meets the inclusive 7.0 threshold.'
    - '[edge:HEALTH-01/adjacency] Baseline/final CodeScene pairs match path and blob; change-set analysis compares productionBaseSha directly to reviewedProductionSha.'
    - '[edge:HEALTH-01/empty] Missing quality_gates/results, provider error, unavailable/stale analysis, or unattributable output blocks HEALTH-01.'
    - '[edge:HEALTH-01/ordering] Staged safeguard identity is captured before commit; parent/tree equality then precedes final change-set and per-file analysis.'
    - '[edge:HEALTH-01/precision] Complete provider output retains revision/blob/path/time/tool; optional metadata is recorded only when returned.'
    - 'DELIV-01: The immutable reviewed product revision has complete product evidence, cleanly rolls back without migration or user repair, leaves relevant source/configuration clean, and is ready to enter native verification, validation, and security.'
  prohibitions: []
  artifacts:
    - path: src/domain/practice/format.ts
      provides: The only two admitted new formatter exports
      exports:
        - formatPracticeUtcMinuteTimestamp
        - formatPracticeMinuteDuration
    - path: src/domain/practice/session-comparison.ts
      provides: Direct CAP-01 caller with its local timestamp body retired
      contains: formatPracticeUtcMinuteTimestamp
    - path: src/hooks/use-practice-session-dashboard.ts
      provides: Direct CAP-01/CAP-02 callers with all three local bodies retired
      contains: formatPracticeMinuteDuration
    - path: src/components/home/home-dashboard.tsx
      provides: Direct CAP-01/CAP-02 callers plus composition-only ownership of the extracted Goals feature
      contains: PracticeGoalsPanel
    - path: src/components/home/practice-goals-panel.tsx
      provides: Cohesive Goals list, mutation orchestration, and panel presentation
      contains: PracticeGoalsPanel
    - path: src/components/home/practice-goal-editor.tsx
      provides: Cohesive goal-editor state, validation presentation, and draft submission
      contains: PracticeGoalEditor
    - path: tests/unit/home-dashboard.test.tsx
      provides: Home timestamp and minute-duration characterization
      contains: characterizes selected Home timestamp and minute-duration formatting
    - path: tests/unit/home-dashboard.test.tsx
      provides: Dashboard-hook comparison and goal characterization
      contains: characterizes dashboard-hook comparison timestamps, durations, and goal wording
    - path: tests/unit/session-comparison.test.ts
      provides: Domain timestamp characterization and excluded seconds-scale regression
      contains: characterizes UTC-minute session-comparison timestamps without changing seconds-scale duration
    - path: tests/unit/architecture-boundaries.test.ts
      provides: Executable boundary proving Home no longer owns Goals implementation details
      contains: practice-goals-panel
  key_links:
    - from: src/domain/practice/session-comparison.ts
      to: src/domain/practice/format.ts
      via: Direct formatPracticeUtcMinuteTimestamp import and calls
      pattern: formatPracticeUtcMinuteTimestamp
    - from: src/hooks/use-practice-session-dashboard.ts
      to: src/domain/practice/index.ts
      via: Existing practice barrel imports both canonical formatters
      pattern: formatPractice(UtcMinuteTimestamp|MinuteDuration)
    - from: src/components/home/home-dashboard.tsx
      to: src/domain/practice/index.ts
      via: Existing practice barrel imports both canonical formatters and retains the analytics fallback
      pattern: formatPractice(UtcMinuteTimestamp|MinuteDuration)
    - from: src/components/home/home-dashboard.tsx
      to: src/components/home/practice-goals-panel.tsx
      via: Home composes the Goals feature through one sibling-component boundary
      pattern: PracticeGoalsPanel
    - from: src/components/home/practice-goals-panel.tsx
      to: src/components/home/practice-goal-editor.tsx
      via: The panel delegates editor-local form state and validation presentation
      pattern: PracticeGoalEditor
---

<objective>
Preserve the completed formatter consolidation, repair the failed Home health boundary by extracting the Goals responsibility into two cohesive sibling components, and prove one behavior-preserving, strictly net-negative combined immutable revision.

Purpose: Satisfy FMT-01, FMT-02, EVID-01, SLIM-01, QUAL-01, HEALTH-01, and DELIV-01 through immutable historical T1/T2 evidence plus the approved CAP-03 repair, without rerunning discovery or lowering Home `7.0`.
Output: Preserved `ef98c28`, an executable RED architecture boundary, a composition-only Home Goals seam, removal of duplicate plumbing where behavior permits, one explicit exact-revision evidence retry, combined-range net-negative proof, rollback proof, and readiness to enter native verification/validation/security.
</objective>

<execution_context>
@C:/Users/wsuto/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wsuto/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_contract evidence_receipt=".planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md" receipt_sha256="316b9a59c3fc8f229f41dd4162c2fb54c213d7e0a3e740b2f70bb9941586d8a5" no_auto_retry="true" />

<context>
@AGENTS.md
@skills/metronome-policy/SKILL.md
@.planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md
@.codescene/quality-gate-policy.md
@src/domain/practice/format.ts
@src/domain/practice/session-comparison.ts
@src/hooks/use-practice-session-dashboard.ts
@src/components/home/home-dashboard.tsx
@tests/unit/home-dashboard.test.tsx
@tests/unit/session-comparison.test.ts
@tests/unit/architecture-boundaries.test.ts
</context>

<assumptions>
- FLAGGED A-01 — RESOLVED: DELIV-01 ends when the reviewed product is ready to enter native verification/validation/security. Native execute-plan marks frontmatter requirements before VERIFICATION; later passing VERIFICATION, current Nyquist VALIDATION, and SECURITY are mandatory ship preconditions with no Phase 1 requirement credit. Milestone Release Exit remains separate. Edge accounting is 24 surfaced edge items = 23 unique edge truths + A-01; the ordinary DELIV-01 truth is outside edge accounting.
- Assumption delta: noun = practice presentation formatter ownership; decision = no-change; “fallback” is existing display text, not an identity/source/model transition.
- Prohibition accounting is 0 = 0 authored + 0 flagged; no bespoke value/safety/ethics descriptor was invented.
- Managed Windows: run validate:debt-gates, lint:debt:changed, and hook-bearing commits elevated. A non-elevated settings/tempfile loop is EXECUTION_ENVIRONMENT_BLOCKED, never a product outcome.
- Historical execution boundary: T1/T2 are complete at `ef98c28`; the first T3 stopped at Home `6.86`. Only T4 and one explicit T5 retry remain executable.
- CAP-03 does not alter domain/service contracts, persistence, dependencies, validation schema, or the goal-ID algorithm. Any such need blocks for an explicit scope decision.
</assumptions>

## Controller pre-dispatch preflight — lifecycle/tooling only

Before dispatch, run native `init.execute-phase 1 --raw`, native plan-structure verification, and the repository liveness command from `AGENTS.md`. Reinstall/rebake OpenGSD only if init explicitly reports missing agents or bake drift; a successful init is cached evidence and must not trigger reinstall. Record the non-empty result of `git symbolic-ref -q --short HEAD` as `IMPLEMENTATION_BRANCH`; detached HEAD is a block. Start one controller-owned observability run. A completed cache key cannot run again, and interruption is recorded without automatic restart.

<historical_execution>
T1 and T2 below completed at immutable `ef98c28`; the first T3 stopped at Home `6.86`. These records are retained for traceability and are not executable tasks.

<historical_task id="01-01-T1">
  <name>Task 01-01-T1: Lock characterization and immutable baselines</name>
  <files>tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts</files>
  <read_first>AGENTS.md, skills/metronome-policy/SKILL.md, .planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md, tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts, src/domain/practice/format.ts, src/domain/practice/session-comparison.ts, src/hooks/use-practice-session-dashboard.ts, src/components/home/home-dashboard.tsx</read_first>
  <action>Open the named observability step. Require the receipt SHA-256 from `execution_contract`, compute the four current fingerprints, and require exact receipt equality. Drift is EVIDENCE_STALE: record blocked and return only the affected CAP to research; do not reproduce external evidence discovery or retry. Capture PRODUCTION_BASE_SHA/src tree/four source blobs. Add only the three receipt-named public-seam cases, prove focused green and four one-at-a-time mutation-red/restoration cycles, restore exact source blobs, and record focused latency. Keep the characterized tests uncommitted for the single final implementation transaction, then complete the step.</action>
  <verify><automated>
&amp; .\scripts\npm-local.ps1 --% run test:unit -- tests/unit/home-dashboard.test.tsx tests/unit/session-comparison.test.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - Three exact named cases cover the full matrix and excluded seconds-scale outputs; focused green, four mutation-red results, and exact restorations are retained.
    - Product/dependency/search/policy fingerprints match the receipt; measured focused latency and all mutation/restoration outcomes are logged.
    - Only the two approved test paths change; no product path changes before characterization is green and restored.
  </acceptance_criteria>
  <done>Behavior and immutable baselines are locked without any external wait, product edit, commit, hook run, or evidence rediscovery.</done>
</historical_task>

<historical_task id="01-01-T2">
  <name>Task 01-01-T2: Consolidate the admitted formatters atomically</name>
  <files>src/domain/practice/format.ts, src/domain/practice/session-comparison.ts, src/hooks/use-practice-session-dashboard.ts, src/components/home/home-dashboard.tsx</files>
  <read_first>skills/metronome-policy/SKILL.md, .planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md, tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts, src/domain/practice/format.ts, src/domain/practice/index.ts, src/domain/practice/session-comparison.ts, src/hooks/use-practice-session-dashboard.ts, src/components/home/home-dashboard.tsx</read_first>
  <action>Require the completed T1 cache key and unchanged receipt fingerprints, then open the named product step. Add only the two approved exports/signatures, rewire seven timestamp plus three minute calls, retain analytics trim/fallback and exclusions, and delete all seven named bodies/wrapper. With the complete index initially empty, stage exactly the four approved source modifications so changed-file gates inspect the real candidate; leave the two test modifications unstaged. Run the seven local commands below once and complete the step with the same exact four-row source index. Do not invoke an external provider, commit, push, broaden scope, or return to discovery.</action>
  <verify><automated>
&amp; .\scripts\npm-local.ps1 --% run test:unit -- tests/unit/home-dashboard.test.tsx tests/unit/session-comparison.test.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run test:unit -- tests/unit/architecture-boundaries.test.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run validate:debt-gates
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint:debt:changed
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint:xo:changed
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run typecheck
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - Exactly two exports, ten direct calls, and zero retired-symbol matches exist; excluded contracts and formatter/package/barrel inputs are unchanged.
    - The seven local candidate gates pass once; the index is exactly four approved source M rows and the worktree has exactly two approved test modifications.
    - No provider, hook-bearing commit, push, dependency, or additional path is introduced in T2.
  </acceptance_criteria>
  <done>One bounded six-file candidate is ready for the single final transaction.</done>
</historical_task>

<historical_task id="01-01-T3">
  <name>Task 01-01-T3: Prove reviewedProductionSha and product readiness</name>
  <files>None — immutable revision, external provider, and disposable rollback evidence</files>
  <read_first>AGENTS.md, skills/metronome-policy/SKILL.md, .planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md, .codescene/quality-gate-policy.md, src/domain/practice/format.ts, src/domain/practice/session-comparison.ts, src/hooks/use-practice-session-dashboard.ts, src/components/home/home-dashboard.tsx, tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts, prettier.config.mjs, package.json, package-lock.json</read_first>
  <action>Open one controller-owned final step. Require a named branch, an exact four-row approved source index, exactly two approved unstaged test modifications, and no other source/test/config drift. Bind one CodeScene pre-commit safeguard to the exact staged source identity, then stage the two tests and require exactly six M rows. Commit once through the elevated full hook; its single run is the authoritative full lint/typecheck/unit/build/debt evidence. Resolve that commit once as reviewedProductionSha and require branch tip equality. Run the receipt's exact 89-line Unicode-safe virtual-normalized LOC recipe, semantic deletion/hunk audit, fresh baseline/final per-file CodeScene and direct baseline-to-final change-set analysis. Preserve exact refs/blobs/provider identity and require no decline, no new severe finding, Home at least 7.0, and net production LOC &lt; 0. Reverse only the product commit in a disposable verification context, prove the baseline source tree plus focused green, dispose it, and recheck the unchanged named branch/tip and clean scope. Complete the external step; provider interruption records `interrupted` and stops without automatic retry.</action>
  <verify><automated>
git diff --cached --quiet
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check "$productionBaseSha..$reviewedProductionSha"
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - IMPLEMENTATION_BRANCH remains named and points to reviewedProductionSha before/after T3 and immediately before native closeout; rollback exists only in a disposed context.
    - The exact 89-line recipe passes under PowerShell 5.1 and retains all branch/revision/config/tool/blob/row/count/hunk/cleanup evidence with normalized net &lt; 0.
    - Every hunk is admitted-only; deletion searches are empty; CodeScene has no decline/new severe finding and Home is at least 7.0.
    - reviewedProductionSha has complete product evidence, clean rollback, and clean scoped state ready to enter native verification/validation/security.
  </acceptance_criteria>
  <done>DELIV-01 product readiness is proven without claiming any later native-gate or Release Exit fact.</done>
</historical_task>
</historical_execution>

<tasks>
<task type="auto" id="01-01-T4" gate_stage="task" budget="heavy" plane="product" resumable="true" resume_key="r01-home-goals-repair" cache_key="approved-home-goals-repair:v1" inputs="ef98-home,ef98-hook,home-tests,architecture-tests,amended-receipt" outputs="home,hook,goals-panel,goal-editor,two-tests">
  <name>Task 01-01-T4: Extract the Goals responsibility without moving the debt</name>
  <files>src/components/home/home-dashboard.tsx, src/hooks/use-practice-session-dashboard.ts, src/components/home/practice-goals-panel.tsx, src/components/home/practice-goal-editor.tsx, tests/unit/home-dashboard.test.tsx, tests/unit/architecture-boundaries.test.ts</files>
  <read_first>.planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md, src/components/home/home-dashboard.tsx, src/hooks/use-practice-session-dashboard.ts, src/components/home/session-comparison-panel.tsx, src/domain/practice/validation.ts, src/services/practice-goals/service.ts, tests/unit/home-dashboard.test.tsx, tests/unit/architecture-boundaries.test.ts</read_first>
  <action>Open the named observability step in run `r01-home-repair-20260721-01`. First add one architecture-boundary test that requires Home to import the Goals panel and forbids Home-owned Goals panel/row/editor/validation/ID implementation details; run that focused test and retain the expected RED output before any production edit. Then use the existing sibling-component pattern to create exactly the admitted panel and editor files, leave Home as composition, preserve all rendered and mutation behavior, and remove duplicate save/delete callback aliases plus the empty-data reference sentinel only where focused tests prove compatibility. Do not change domain/service/schema/persistence/dependencies or the goal-ID algorithm. Run focused Home/architecture tests, then the repository-local candidate checks once. Record exact timing, token dimensions, inputs, outputs, cache/resume identity, and soft-budget status. Any need outside the six-file repair surface blocks for user scope decision; do not restart discovery or select another target.</action>
  <verify><automated>
&amp; .\scripts\npm-local.ps1 --% run test:unit -- tests/unit/home-dashboard.test.tsx tests/unit/architecture-boundaries.test.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run validate:debt-gates
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint:debt:changed
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint:xo:changed
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run lint
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\scripts\npm-local.ps1 --% run typecheck
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - The architecture test is observed RED before production edits and GREEN afterward; existing Home behavior tests remain green.
    - Home imports/composes `PracticeGoalsPanel` and contains no Goals panel/row/editor/draft-validation/ID implementation body.
    - The implementation uses exactly two cohesive new components rather than relocating one monolithic block; duplicate callback aliases and the reference sentinel are absent when compatible.
    - The repair diff relative to `ef98c28` contains exactly two added production files plus four modified approved files and no domain/service/dependency/persistence change.
  </acceptance_criteria>
  <done>One behavior-locked, bounded Goals repair is ready for strict task review and the single final evidence retry.</done>
</task>

<task type="auto" id="01-01-T5" gate_stage="pre_merge" budget="external" plane="external" resumable="true" resume_key="r01-home-final-evidence-retry" cache_key="immutable-home-repair-final:v1" retry_of="r01-formal-20260721-01:t3-final-evidence" inputs="3370-baseline,ef98-intermediate,t4-six-files,formatter-inputs" outputs="reviewed-revision,combined-loc,health-evidence,rollback" external_job_id="codescene-r01-home-final-retry">
  <name>Task 01-01-T5: Perform the one approved final-evidence retry</name>
  <files>None — immutable revision, external provider, combined-range verifier, and disposable rollback evidence</files>
  <read_first>AGENTS.md, skills/metronome-policy/SKILL.md, .planning/phases/01-canonical-practice-presentation-formatting/01-EXECUTION-RECEIPT.md, .codescene/quality-gate-policy.md, src/domain/practice/format.ts, src/domain/practice/session-comparison.ts, src/hooks/use-practice-session-dashboard.ts, src/components/home/home-dashboard.tsx, src/components/home/practice-goals-panel.tsx, src/components/home/practice-goal-editor.tsx, tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts, tests/unit/architecture-boundaries.test.ts, prettier.config.mjs, package.json, package-lock.json</read_first>
  <action>After strict task review, open the named external step and record `retry_of`. Require the exact six-path repair diff relative to `ef98c28`, no other drift, and a named delivery branch state consistent with the approved main-branch workflow. Bind one staged safeguard to the exact repair identity, then commit through the elevated full pre-commit hook without bypass. Resolve the resulting commit once as reviewedProductionSha. Against immutable baseline `3370d2f93fd6740d96150d9ee69e31238b258c6a`, run the amended concise normalized LOC verifier, require exact source inventory `4M + 2A`, and require total production net below zero. Run fresh per-file and direct change-set analysis once: existing changed sources may not decline or gain severe findings, both new Goals files require attributable no-severe results, and Home must be at least `7.0`. Retain exact refs/blobs/provider output plus timing/token/input/output data under the run log. Prove rollback in a disposable context, then restore/check the unchanged delivery revision and clean scope. Failure or interruption terminates the retry without threshold reduction, rediscovery, alternate target, or another plan cycle.</action>
  <verify><automated>
git diff --cached --quiet
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check "3370d2f93fd6740d96150d9ee69e31238b258c6a..$reviewedProductionSha"
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - The elevated hook passes once for the repair commit, and reviewedProductionSha stays the approved delivery revision before and after disposable rollback.
    - The combined normalized production result over `3370d2f..reviewedProductionSha` is strictly negative with exact `4M + 2A` source rows and per-path counts retained.
    - Existing changed sources do not decline or gain severe findings; new Goals files have no severe finding; Home is at least `7.0`; provider evidence is attributable and complete.
    - Scope is clean and the immutable revision is ready for native verification/validation/security without claiming later delivery facts.
  </acceptance_criteria>
  <done>The sole approved retry proves or blocks DELIV-01; it never loops back to discovery or another repair automatically.</done>
</task>
</tasks>

## Native lifecycle routing — outside requirement credit

Native execute-plan marks the seven frontmatter requirements complete before VERIFICATION, so immutable historical T1/T2 evidence plus amended T4/T5 evidence—not later artifacts—must satisfy them. After `01-01-SUMMARY.md`, passing `01-VERIFICATION.md`, current Nyquist `01-VALIDATION.md`, and `01-SECURITY.md` with `threats_open: 0` are mandatory `$gsd-ship` preconditions but receive no Phase 1 requirement credit. `$gsd-ship` and every Milestone Release Exit fact remain subsequent routing; this plan credits no CI, delivery, or clean-main fact.

<threat_model>
## STRIDE Threat Register — ASVS L1, block_on high
| Threat | Category | Severity | Mitigation |
|--------|----------|----------|------------|
| T-01-01 | Formatting tampering | medium | Characterize invalid/threshold behavior; retain fixed UTC/fallback/floor policy without raw-input rendering. |
| T-01-02 | Surface/supply-chain tampering | high | Exact complete-index allowlists, immutable formatter/package inputs, no dependency, elevated full hooks. |
| T-01-03 | Evidence repudiation | medium | Immutable SHA/tree/blob/branch/path/time/tool and staged-to-commit identity bindings. |
| T-01-04 | Tool/provider availability | low | Fail closed as EXECUTION_ENVIRONMENT_BLOCKED/provider blocked; never convert availability into product evidence. |
</threat_model>

## Artifacts this phase produces

- The preserved two formatter exports, three formatter characterization cases, one Goals architecture-boundary case, exactly two cohesive Goals components, and `01-01-SUMMARY.md`.
- No new dependency, generic wrapper/facade, feature flag, service, domain owner, persistence/runtime path, seed, candidate, or historical artifact; only the two named CAP-03 production paths are added.
- VERIFICATION, current Nyquist VALIDATION, and SECURITY are later native ship-precondition artifacts, not plan deliverables or requirement evidence.

## Multi-Source Coverage Audit

| Source | Contract | Coverage |
|--------|----------|----------|
| GOAL/FMT-01/FMT-02 | Exact presentation, ten calls, seven deletions, one formatter owner | historical T1-T2, T5 |
| EVID-01/SLIM-01 | Immutable evidence and strict combined normalized net-negative proof | historical T1-T2, T5 |
| QUAL-01/HEALTH-01 | Characterization, Goals boundary, full gates, exact-revision health | historical T1-T2, T4-T5 |
| DELIV-01 | Complete immutable product evidence, rollback, delivery-revision identity, cleanliness, ready to enter native gates | T5 |
| RESEARCH CAP-01/CAP-02/CAP-03 | Historical formatter surface plus exact Goals repair surface and exclusions | historical T1-T2, T4-T5 |
| CONTEXT | No CONTEXT/SPEC by explicit approval; current research binds scope | N/A |

<verification>
- [ ] The controller gets a clean native init, native structure pass, and repository liveness pass before dispatch; reinstall/rebake occurs only on explicit native drift.
- [x] Historical T1/T2 characterization and formatter consolidation are immutable at `ef98c28`; they are not dispatched again.
- [ ] T4 records architecture RED before the Goals repair, then proves focused GREEN and the exact six-path repair boundary.
- [ ] Native artifacts/key links are discovered nonzero at planning time; their final-state checks all pass only after implementation/native verification.
- [ ] T5 binds all product evidence and disposable rollback to reviewedProductionSha, proves exact combined `4M + 2A` source inventory and net-negative normalized LOC, and performs the one permitted health retry.
- [ ] Edge accounting is 24 surfaced = 23 unique edge truths + resolved A-01; ordinary DELIV-01 truth is outside edge accounting; prohibitions are 0 = 0 + 0.
- [ ] Later VERIFICATION/VALIDATION/SECURITY pass before `$gsd-ship` without being credited to Phase 1 requirements.
</verification>

<success_criteria>
- All seven requirements are satisfied by immutable historical T1/T2 plus T4/T5 product/repository evidence; none depends on a later native-gate or Release Exit fact.
- Behavior/exclusions remain exact; the combined six-source range is admitted-only, healthy, reversible, clean, delivery-revision-bound, and strictly normalized net-negative.
</success_criteria>

<output>
Create `01-01-SUMMARY.md`, then enter native verification/validation/security routing without crediting those outcomes or later shipping to this plan.
</output>
