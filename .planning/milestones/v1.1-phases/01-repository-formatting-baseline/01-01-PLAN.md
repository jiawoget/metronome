---
phase: 01-repository-formatting-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .prettierignore
  - prettier.config.mjs
  - package.json
  - package-lock.json
  - .github/workflows/ci.yml
  - "src/**"
  - "tests/**"
  - "scripts/**"
  - "docs/**"
  - "other tracked Prettier-supported text files selected from the repository root outside .planning/**"
autonomous: true
requirements:
  - POLICY-01
  - TOOL-01
  - TOOL-02
  - EVID-01
  - BASE-01
  - BASE-02
  - BASE-03
  - WIN-01
  - WIN-02
  - ENF-01
  - ENF-02
  - QUAL-01
  - HIST-01
  - DELIV-01
must_haves:
  truths:
    - "Per D-05 through D-08, one root LF/Prettier policy uses the retained exact Prettier 3.9.5 and Tailwind plugin 0.8.0 artifacts, exposes only format and format:check, and never acquires a package."
    - "Per D-02 and D-09, the canonical root commands cover every allowed tracked Prettier-supported text file while all .planning/** lifecycle bytes remain outside formatter traversal and the nested deprecated quarantine is never consumed."
    - "Per D-10 through D-12, one independently revertible commit contains only formatter output and has reached a real fixed point before commit."
    - "Per D-13 through D-16, Windows uses a supported direct npm route or the existing scripts/npm-local.ps1 fallback, the tracked hook stays fast, and Ubuntu CI checks formatting before its existing gates."
    - "Per D-17 through D-19, one frozen implementation revision passes the complete non-browser local sequence and is ready for native verification without claiming shipping, merge, synchronized main, or a fresh R01."
  artifacts:
    - path: .gitattributes
      provides: "Repository LF/text classification with binary, generated-output, lifecycle, and quarantine protection"
      contains: "* text=auto eol=lf"
    - path: .prettierignore
      provides: "Formatter traversal boundary excluding all .planning/**, binary, dependency, generated, cache, and output paths"
      contains: ".planning/"
    - path: prettier.config.mjs
      provides: "Sole formatter configuration with LF output and Tailwind v4 stylesheet integration"
      contains: "tailwindStylesheet"
    - path: package.json
      provides: "Exact formatter pins and the only two public formatting commands"
      contains: "format:check"
    - path: package-lock.json
      provides: "Retained exact formatter/plugin resolutions and integrity metadata"
      contains: "prettier-3.9.5.tgz"
    - path: .githooks/pre-commit
      provides: "Existing tracked executable bootstrap-aware fast commit gate"
      contains: "git diff --cached --check"
    - path: .github/workflows/ci.yml
      provides: "Ubuntu format check ordered before lint, typecheck, unit tests, and build"
      contains: "npm run format:check"
    - path: scripts/npm-local.ps1
      provides: "Existing repository-local Node/Corepack/npm fallback retained unchanged"
      contains: "npm@11.17.0"
  key_links:
    - from: package.json
      to: prettier.config.mjs
      via: "npm run format and format:check invoke the local Prettier CLI, which discovers the single root config"
      pattern: "prettier --(write|check) \\. --ignore-unknown"
    - from: prettier.config.mjs
      to: src/app/globals.css
      via: "tailwindStylesheet points the Tailwind plugin at the live v4 stylesheet"
      pattern: "tailwindStylesheet.*src/app/globals\\.css"
    - from: package.json
      to: .prettierignore
      via: "root-directory formatter inference uses the root ignore boundary"
      pattern: "prettier --(write|check) \\."
    - from: .githooks/pre-commit
      to: package.json
      via: "the resolved direct-or-wrapper npm route runs the canonical format:check script after bootstrap"
      pattern: "run_npm run format:check"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "Ubuntu CI runs format:check immediately after npm ci and before lint"
      pattern: "npm run format:check"
---

<objective>
Establish and prove the repository-formatting baseline defined by POLICY-01 through DELIV-01 while honoring every locked decision D-01 through D-19.

Purpose: Give maintainers one deterministic Windows/Ubuntu formatting contract whose repository-wide mechanical result is isolated, idempotent, reviewable, and behavior-preserving.
Output: Root LF/Prettier policy, exact retained tool pins, canonical npm commands, Ubuntu enforcement, one formatter-only fixed-point baseline commit, and frozen-candidate evidence.
</objective>

<execution_context>
@C:/Users/wsuto/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wsuto/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@skills/metronome-policy/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/01-repository-formatting-baseline/01-CONTEXT.md
@.planning/phases/01-repository-formatting-baseline/01-RESEARCH.md
@package.json
@package-lock.json
@prettier.config.mjs
@.gitignore
@.githooks/pre-commit
@.github/workflows/ci.yml
@scripts/npm-local.ps1
@src/app/globals.css

<planning_notes>
- The live `.planning/config.json` explicitly sets `security_enforcement: false`; that locked, current configuration supersedes the stale RESEARCH.md sentence claiming the key is absent. This tooling plan therefore emits no product-domain threat model.
- The `metronome-policy` evidence lane is not applicable to pure formatting output, but its reuse and no-parallel-lifecycle rules bind every task: retain the installed formatter, tracked hook, and npm fallback rather than adding owners.
- `workflow.specless_probe_fallback=false` is an intentional visible choice. Verification below uses only source-defined policy, fixed-point, history, runtime, hook, CI, and quality checks; it does not synthesize edge or prohibition probes.
- No prior milestone SUMMARY is a dependency. The history digest contains product-import phases only and provides no formatting owner or pattern that this plan should reuse.
</planning_notes>
</context>

## Artifacts this phase produces

- New root paths: `.gitattributes` and `.prettierignore`.
- New configuration keys in `prettier.config.mjs`: `endOfLine: "lf"` and `tailwindStylesheet: "./src/app/globals.css"`.
- New public npm scripts in `package.json`: `format` and `format:check`; no other formatter script is created.
- Exact retained manifest/root-lock specifiers: `prettier` 3.9.5 and `prettier-plugin-tailwindcss` 0.8.0. Existing package resolution URLs and integrity values remain unchanged.
- New Ubuntu CI step: `npm run format:check` after dependency installation and before lint.
- One isolated Git commit containing the complete fixed-point formatter output for the configured allowed surface.
- Existing owners reused without replacement: executable `.githooks/pre-commit`, local `core.hooksPath=.githooks`, and `scripts/npm-local.ps1`.
- No application type, function, component, service, API, schema, route, persistence model, or other code symbol is created.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the canonical formatter path from root policy through Ubuntu enforcement</name>
  <precondition>The existing installed artifacts resolve to Prettier 3.9.5 and prettier-plugin-tailwindcss 0.8.0, and scripts/npm-local.ps1 is present; if any is missing, stop rather than acquiring a package.</precondition>
  <reversibility rating="costly">Changing this repository-wide policy later requires a coordinated reformat plus hook and CI adjustment, but no data migration or published product-contract break.</reversibility>
  <files>.gitattributes, .prettierignore, prettier.config.mjs, package.json, package-lock.json, .github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/01-repository-formatting-baseline/01-CONTEXT.md` decisions D-01 through D-08 and D-13 through D-16
    - `.planning/phases/01-repository-formatting-baseline/01-RESEARCH.md` sections Standard Stack, Package Legitimacy Audit, Patterns 1-3 and 5, and Pitfalls 3-6
    - `package.json` scripts, engine contract, and formatter dependency specifiers
    - `package-lock.json` root package record plus `node_modules/prettier` and `node_modules/prettier-plugin-tailwindcss` records
    - `prettier.config.mjs`, `.gitignore`, `src/app/globals.css`, `.githooks/pre-commit`, `.github/workflows/ci.yml`, and `scripts/npm-local.ps1`
  </read_first>
  <action>
Stay on the current primary checkout and current formatting branch per D-01, D-04, and D-04a. Do not create a worktree, lifecycle helper, controller, validator, scanner, cache, receipt, ledger, or telemetry path per D-03. Treat `.planning/**` only as an excluded path and never enter or consume the nested deprecated quarantine per D-02.

Create `.gitattributes` per D-05 with `* text=auto eol=lf`, explicit `-text` rules for `.planning/**` and the nested quarantine, explicit `-text` rules for dependency/generated/output directories, and explicit `binary` rules for the image, font, audio, video, archive, PDF, icon, and WASM extensions enumerated in RESEARCH.md Pattern 3. Create `.prettierignore` per D-06 and D-09 with distinct comments/rules for all `.planning/` lifecycle output and its nested absolute quarantine, then exclude `node_modules/`, `.tools/`, `.next/`, `out/`, coverage/test reports, `.audit/`, `.tmp/`, `.code-review-graph/`, `.logs/`, `next-env.d.ts`, TypeScript build info, and the same binary extension set. Do not enumerate quarantined contents while creating or checking these policy files.

Extend the sole `prettier.config.mjs` owner per D-06 and D-07: preserve semicolons, double quotes, no trailing commas, and the existing sole plugin; add `endOfLine: "lf"` and `tailwindStylesheet: "./src/app/globals.css"`. In `package.json`, add exactly `format: "prettier --write . --ignore-unknown"` and `format:check: "prettier --check . --ignore-unknown"`; exact-pin only the already installed `prettier` 3.9.5 and `prettier-plugin-tailwindcss` 0.8.0 per D-08. Mirror those two exact root specifiers in `package-lock.json` while preserving its existing 3.9.5/0.8.0 package entries, tarball URLs, and SHA-512 integrity values. Do not run install, update, audit-fix, or any package-acquisition command.

Add one `Check formatting` step to `.github/workflows/ci.yml` per D-16 immediately after `npm ci` and before the existing lint step; retain the remaining lint, typecheck, unit, and build order. Reuse `.githooks/pre-commit` and `scripts/npm-local.ps1` per D-13 through D-15: confirm the tracked hook remains mode `100755`, `core.hooksPath` remains `.githooks`, its bootstrap guard skips the full check only while `.prettierignore` is absent from `HEAD`, and its resolved route runs only staged whitespace plus `format:check`. Do not copy or recreate a heavy local hook and do not mutate user or system PATH.

Before committing this tracer, run the retained formatter binary offline only over the task-owned JSON/ESM/YAML files so their semantic policy edits are formatter-clean. Stage exactly `.gitattributes`, `.prettierignore`, `prettier.config.mjs`, `package.json`, `package-lock.json`, and `.github/workflows/ci.yml`, then assert that the staged path set equals those six paths and run `git diff --cached --check`. Do not invoke repository-wide renormalization or accept any attribute-driven change outside those task-owned files; Task 2 is the sole repository-wide mechanical baseline. The bootstrap commit must pass through the existing hook: staged whitespace runs, while the full repository format check is deferred because `.prettierignore` is not yet in `HEAD`.
  </action>
  <acceptance_criteria>
    - POLICY-01, TOOL-01, TOOL-02, EVID-01, WIN-01, WIN-02, ENF-01, and ENF-02 have concrete live artifacts with no package acquisition or second owner.
    - The exact manifest and root lock specifiers are 3.9.5/0.8.0, and the resolved package versions, URLs, and integrity values are unchanged.
    - `.planning/**` is excluded structurally by both root policies without reading any quarantined content.
    - The existing tracked hook remains executable, bootstrap-aware, fast, and fail-closed through one resolved npm route.
    - CI runs `format:check` after install and before every existing quality gate.
    - The tracer commit contains exactly the six task-owned policy/tooling/enforcement paths, contains no repository-wide EOL change, and succeeds through the hook bootstrap path.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
& .\scripts\npm-local.ps1 --% exec --offline -- prettier --check package.json package-lock.json prettier.config.mjs .github/workflows/ci.yml --ignore-unknown
if ($LASTEXITCODE -ne 0) { throw "Task-owned formatter check failed" }
& .\.tools\node-v24.17.0-win-x64\node.exe --input-type=module -e 'import fs from "node:fs"; import config from "./prettier.config.mjs"; const pkg=JSON.parse(fs.readFileSync("package.json","utf8")); const lock=JSON.parse(fs.readFileSync("package-lock.json","utf8")); const prettier=lock.packages["node_modules/prettier"]; const plugin=lock.packages["node_modules/prettier-plugin-tailwindcss"]; if(pkg.devDependencies.prettier!=="3.9.5"||pkg.devDependencies["prettier-plugin-tailwindcss"]!=="0.8.0"||pkg.scripts.format!=="prettier --write . --ignore-unknown"||pkg.scripts["format:check"]!=="prettier --check . --ignore-unknown"||lock.packages[""].devDependencies.prettier!=="3.9.5"||lock.packages[""].devDependencies["prettier-plugin-tailwindcss"]!=="0.8.0"||prettier.version!=="3.9.5"||prettier.integrity!=="sha512-/FVl766LpUfB5vXgCYOYa0MeV/441Ia99AeICQIQFTY/Nw0roZwULcXpku5i1/m5kt/baz+s4Zogspd839HSMg=="||plugin.version!=="0.8.0"||plugin.integrity!=="sha512-V8ITGH87yuBDF6JpEZTOVlUz/saAwqb8f3HRgUj8Lh+tGCcrmorhsLpYqzygwFwK0PE2Ib6Mv3M7T/uE2tZV1g=="||config.endOfLine!=="lf"||config.tailwindStylesheet!=="./src/app/globals.css") throw new Error("Formatter policy or retained lock artifact mismatch");'
if ($LASTEXITCODE -ne 0) { throw "Formatter policy or retained lock artifact verification failed" }
$attributes = Get-Content -Raw -LiteralPath .gitattributes
$ignore = Get-Content -Raw -LiteralPath .prettierignore
if (-not $attributes.Contains("* text=auto eol=lf") -or -not $attributes.Contains(".planning/** -text") -or -not $attributes.Contains(".planning/deprecated/** -text") -or -not $ignore.Contains(".planning/") -or -not $ignore.Contains(".planning/deprecated/") -or -not $ignore.Contains("node_modules/")) { throw "Formatting boundary policy mismatch" }
$ci = Get-Content -Raw -LiteralPath .github/workflows/ci.yml
if (-not $ci.Contains("npm run format:check") -or $ci.IndexOf("npm run format:check") -lt $ci.IndexOf("npm ci") -or $ci.IndexOf("npm run format:check") -gt $ci.IndexOf("npm run lint")) { throw "CI formatting order mismatch" }
$hook = Get-Content -Raw -LiteralPath .githooks/pre-commit
$hookRequired = @("git diff --cached --check", "git cat-file -e HEAD:.prettierignore", "run_npm run format:check", "scripts/npm-local.ps1")
$hookForbidden = @("npm run lint", "npm run typecheck", "npm run test:unit", "npm run build")
if (@($hookRequired | Where-Object { -not $hook.Contains($_) }).Count -ne 0 -or @($hookForbidden | Where-Object { $hook.Contains($_) }).Count -ne 0) { throw "Tracked hook contract mismatch" }
$hookMode = (git ls-files --stage -- .githooks/pre-commit)
if ($LASTEXITCODE -ne 0) { throw "Unable to inspect tracked hook mode" }
$hooksPath = (git config --local --get core.hooksPath)
if ($LASTEXITCODE -ne 0) { throw "Unable to inspect core.hooksPath" }
if (-not $hookMode.StartsWith("100755") -or $hooksPath -ne ".githooks") { throw "Tracked hook activation mismatch" }
$expectedStaged = @(".gitattributes", ".prettierignore", "prettier.config.mjs", "package.json", "package-lock.json", ".github/workflows/ci.yml") | Sort-Object
$actualStaged = @(git diff --cached --name-only --) | Sort-Object
if ($LASTEXITCODE -ne 0) { throw "Unable to inspect tracer staged paths" }
if (@(Compare-Object -ReferenceObject $expectedStaged -DifferenceObject $actualStaged).Count -ne 0) { throw "Tracer staged set is not exactly the six task-owned paths" }
git diff --cached --check
if ($LASTEXITCODE -ne 0) { throw "Tracer staged whitespace check failed" }
    </automated>
  </verify>
  <done>The repository has one committed root formatting contract and one wired Ubuntu check; the existing Windows fallback and fast tracked hook remain the runtime/enforcement owners, and no repository-wide formatter write has occurred yet.</done>
</task>

<task type="auto">
  <name>Task 2: Reach and commit the complete formatter fixed point</name>
  <reversibility rating="reversible">The formatter-only result is isolated in one commit that can be reversed independently without a product data migration or semantic repair.</reversibility>
  <files>Every tracked Prettier-supported file changed by `npm run format` from the repository root after `.prettierignore` exclusions; this includes applicable source, tests, configuration, scripts, current documentation, and legacy documentation, and excludes every `.planning/**` path.</files>
  <read_first>
    - `.planning/phases/01-repository-formatting-baseline/01-CONTEXT.md` decisions D-02 and D-09 through D-12
    - `.planning/phases/01-repository-formatting-baseline/01-RESEARCH.md` Pattern 4, Anti-Patterns, and Pitfalls 1, 2, and 7
    - The committed `package.json`, `.prettierignore`, and `prettier.config.mjs` produced by Task 1
    - `.githooks/pre-commit` for the post-bootstrap commit gate
  </read_first>
  <action>
Begin from the clean allowed worktree produced by the tracer. Run the canonical `npm run format` through a supported direct npm route when one exists, otherwise through `scripts/npm-local.ps1`, per D-13 and D-14. The command must target the repository root exactly as defined in `package.json`; do not create a hand-picked file list and do not run any discovery, hash, search, or evidence command inside `.planning/**` per D-02 and D-09.

Inspect the allowed diff and `git diff --check` to confirm it contains formatter output only: no dependency decision, policy/enforcement edit, generated artifact, manual cleanup, lifecycle metadata, or product-semantic change per D-10 and D-12. Stage only tracked formatter modifications with the `.planning/**`-excluding Git pathspec. Run the same canonical formatter write again and compare the new unstaged allowed diff with the staged prior pass. If formatter output remains, inspect it, stage that formatter-only delta, and run the same write/comparison again until the unstaged allowed diff is empty. The reproduced second-pass locator reflow is normal formatter output; do not hand-edit it, special-case the file, or add a repetition script, cache, hash, ledger, receipt, or validator.

At the fixed point, run `format:check`, inspect the staged formatter-only path set, and commit exactly that staged result as the phase's single mechanical baseline per D-10, D-11, and D-12. Do not amend policy/tooling/enforcement files into this commit. The existing hook now sees `.prettierignore` in `HEAD`, so it must run both staged whitespace validation and the complete canonical `format:check` before the commit succeeds.
  </action>
  <acceptance_criteria>
    - BASE-01 covers the complete configured allowed surface rather than selected source globs.
    - BASE-02 has one non-empty staged/committed formatter-only diff with no task-1 policy, tooling, enforcement, lifecycle, generated, semantic, or manual edit.
    - BASE-03 reaches a real fixed point before commit: another identical write creates no unstaged allowed diff and the complete check passes.
    - HIST-01 is reviewable as one independently revertible mechanical commit; no exact total plan, task, or commit count is treated as a gate.
    - The full hook format check runs on the mechanical commit and succeeds through the retained direct-or-wrapper route.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
& .\scripts\npm-local.ps1 --% run format
if ($LASTEXITCODE -ne 0) { throw "Fixed-point formatter write failed" }
git diff --exit-code -- . ':(exclude).planning/**'
if ($LASTEXITCODE -ne 0) { throw "Formatter has not reached a fixed point" }
& .\scripts\npm-local.ps1 --% run format:check
if ($LASTEXITCODE -ne 0) { throw "Complete formatter check failed" }
git diff --cached --check
if ($LASTEXITCODE -ne 0) { throw "Mechanical baseline staged whitespace check failed" }
git diff --cached --quiet
if ($LASTEXITCODE -eq 0) { throw "Mechanical baseline is unexpectedly empty" }
if ($LASTEXITCODE -gt 1) { throw "Unable to inspect staged baseline" }
$staged = @(git diff --cached --name-only -- . ':(exclude).planning/**')
if ($LASTEXITCODE -ne 0) { throw "Unable to inspect mechanical baseline staged paths" }
$policyPaths = @(".gitattributes", ".prettierignore", "prettier.config.mjs", "package.json", "package-lock.json", ".githooks/pre-commit", ".github/workflows/ci.yml", "scripts/npm-local.ps1")
if (@($staged | Where-Object { $policyPaths -contains $_ }).Count -ne 0) { throw "Mechanical baseline contains policy, tooling, enforcement, or runtime files" }
    </automated>
  </verify>
  <done>One committed formatter-only baseline covers the complete allowed surface, survives the full tracked hook, and is already at the same fixed point the post-commit proof will exercise.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the implementation revision and record complete non-browser evidence</name>
  <files>.planning/phases/01-repository-formatting-baseline/01-01-SUMMARY.md (command evidence recorded after implementation; no application file changes)</files>
  <read_first>
    - `AGENTS.md` Git Hook and Release Exit sections
    - `.planning/REQUIREMENTS.md` QUAL-01, HIST-01, DELIV-01, and Milestone Release Exit
    - `.planning/phases/01-repository-formatting-baseline/01-CONTEXT.md` decisions D-13 through D-19
    - `.planning/phases/01-repository-formatting-baseline/01-RESEARCH.md` Code Examples and Environment Availability
    - The committed `package.json`, `.githooks/pre-commit`, `.github/workflows/ci.yml`, and `scripts/npm-local.ps1`
  </read_first>
  <action>
Freeze the current implementation revision after the mechanical commit and record its exact commit ID. Resolve the supported Windows npm route without changing PATH: use direct `npm`/`npm.cmd` only if it satisfies the repository engine contract, otherwise use the existing `scripts/npm-local.ps1` route per D-13 and D-14. Confirm the tracked hook remains mode `100755`, local `core.hooksPath` remains `.githooks`, and CI still places the canonical format check before lint per D-15 and D-16.

Run the source-defined final local sequence exactly once on that frozen revision per D-17: `format:check`, then a second canonical `format` write followed by an allowed-surface no-diff assertion, then lint, typecheck, the full unit suite, and build. Do not run Playwright, a browser, visual checks, microphone checks, or product UAT. Any failed final gate invalidates the entire candidate: diagnose and repair formatter/check/no-diff failures in the mechanical task boundary and lint/typecheck/unit/build failures in the owning policy, enforcement, or formatter-output boundary, establish a replacement frozen revision, then rerun the complete sequence once on that replacement. Continue bounded in-phase diagnosis and repair rather than deferring an in-scope failure; escalate only for a material scope or architecture change or unavailable external authority.

Prove HIST-01 and DELIV-01 without creating an evidence subsystem: inspect the mechanical commit's parent/diff to confirm policy/tooling/enforcement paths are absent, use Git's reverse-apply check to prove the isolated commit can be cleanly rolled back, confirm the implementation revision did not change during verification, and confirm the allowed worktree is clean. Record the exact revision, chosen runtime route/version, command outcomes, mechanical commit identity, rollback check, hook/CI facts, and no-product-change basis in `01-01-SUMMARY.md`.

Honor D-03, D-04, and D-04a throughout: remain in the primary checkout and use native execution/verification with no controller or routine checkpoint. Per D-18 and D-19, do not create or merge a pull request, claim exact-final-PR-head review/CI, synchronize `main`, start another milestone, or begin a fresh R01; those are separate release-exit facts after native phase verification.
  </action>
  <acceptance_criteria>
    - QUAL-01 passes on one immutable implementation revision: canonical check, second-write no-diff, lint, typecheck, full unit suite, and build all succeed without Playwright.
    - HIST-01 identifies one isolated mechanical commit and confirms its reverse patch applies cleanly; policy/tooling/enforcement remain in a separate earlier commit.
    - DELIV-01 records the exact implementation revision, route/version, allowed-scope and quarantine-policy evidence, fixed point, hook/CI order, quality gates, clean allowed worktree, and rollback proof.
    - No product behavior, application symbol, dependency acquisition, lifecycle parallelism, worktree, release-exit action, or deprecated-planning consumption occurs.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
$frozenRevision = (git rev-parse HEAD).Trim()
if ($LASTEXITCODE -ne 0) { throw "Unable to resolve frozen implementation revision" }
& .\scripts\npm-local.ps1 --% run format:check
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate format check failed" }
& .\scripts\npm-local.ps1 --% run format
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate second formatter write failed" }
git diff --exit-code -- . ':(exclude).planning/**'
if ($LASTEXITCODE -ne 0) { throw "Frozen revision changed on the second formatter write" }
& .\scripts\npm-local.ps1 --% run lint
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate lint failed" }
& .\scripts\npm-local.ps1 --% run typecheck
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate typecheck failed" }
& .\scripts\npm-local.ps1 --% run test:unit
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate unit suite failed" }
& .\scripts\npm-local.ps1 --% run build
if ($LASTEXITCODE -ne 0) { throw "Frozen-candidate build failed" }
$reversePatch = @(git show --format= --binary $frozenRevision -- . ':(exclude).planning/**')
if ($LASTEXITCODE -ne 0) { throw "Unable to read mechanical commit patch" }
$reversePatch | git apply --reverse --check -
if ($LASTEXITCODE -ne 0) { throw "Mechanical commit does not reverse-apply cleanly" }
$verifiedRevision = (git rev-parse HEAD).Trim()
if ($LASTEXITCODE -ne 0) { throw "Unable to re-read implementation revision" }
if ($verifiedRevision -ne $frozenRevision) { throw "Implementation revision changed during verification" }
$allowedStatus = @(git status --porcelain=v1 --untracked-files=all -- . ':(exclude).planning/**')
if ($LASTEXITCODE -ne 0) { throw "Unable to inspect allowed worktree status" }
if ($allowedStatus.Count -ne 0) { throw "Allowed worktree is not clean after verification" }
    </automated>
  </verify>
  <done>The frozen implementation revision has complete, revision-bound, non-browser evidence and is ready for native phase verification; no post-verification release-exit fact is claimed.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One deterministic, safe, Windows/Ubuntu repository formatting baseline with fast commit enforcement and no behavior change | T1-T3 | COVERED | Policy, fixed-point baseline, and frozen evidence form one executable path. |
| REQ | POLICY-01 | Root LF policy; binary/generated/planning protection | T1 | COVERED | `.gitattributes` plus structural no-consume boundary. |
| REQ | TOOL-01 | One root config/ignore with Tailwind v4 integration | T1 | COVERED | Existing owner extended, no second config. |
| REQ | TOOL-02 | Exact retained pins; only two public commands; no second formatter/workflow | T1 | COVERED | No install task or package checkpoint. |
| REQ | EVID-01 | Current native research resolves official identities, integrity, compatibility, and repository conflicts | T1 | COVERED | Task consumes the HIGH-confidence research and its package audit. |
| REQ | BASE-01 | Complete allowed tracked supported surface | T2 | COVERED | Root command plus ignore boundary, never selected globs. |
| REQ | BASE-02 | Formatter-only isolated baseline | T2-T3 | COVERED | Staged path/content inspection plus history proof. |
| REQ | BASE-03 | Fixed point before commit and no-diff/check proof after | T2-T3 | COVERED | Repeated real writes before commit; post-commit proof on frozen revision. |
| REQ | WIN-01 | Supported direct runtime or existing local fallback; no PATH mutation | T1,T3 | COVERED | Existing wrapper retained and route recorded. |
| REQ | WIN-02 | Hook resolves npm/npm.cmd then PowerShell wrapper | T1,T3 | COVERED | Existing hook reused and statically/runtime verified. |
| REQ | ENF-01 | Fast bootstrap-aware hook only | T1-T2 | COVERED | Tracer exercises bootstrap; baseline exercises full check. |
| REQ | ENF-02 | Ubuntu format check before existing gates | T1,T3 | COVERED | One CI step and final order check. |
| REQ | QUAL-01 | Frozen format/no-diff/lint/typecheck/unit/build once; no browser | T3 | COVERED | One source-defined sequence on exact revision. |
| REQ | HIST-01 | Mechanical commit isolated without count gate | T2-T3 | COVERED | Content/parent/reverse-apply evidence. |
| REQ | DELIV-01 | Immutable revision with scope/runtime/gate/rollback evidence | T3 | COVERED | SUMMARY records exact evidence; shipping remains separate. |
| RESEARCH | STACK | Retain Prettier 3.9.5/plugin 0.8.0, existing Git and wrapper | T1 | COVERED | No acquisition or upgrade. |
| RESEARCH | CONFIG | LF plus Tailwind v4 stylesheet and root `--ignore-unknown` commands | T1 | COVERED | Exact config keys/commands specified. |
| RESEARCH | BOUNDARY | `.planning/**` lifecycle exclusion differs from nested absolute quarantine | T1-T3 | COVERED | Policy comments/rules distinguish them; no content probe exists. |
| RESEARCH | FIXED-POINT | Index-based stage/write/compare procedure resolves reproduced second-pass reflow | T2 | COVERED | Repeats actual formatter writes without custom automation or manual edit. |
| RESEARCH | HOOK | Existing tracked executable hook and `core.hooksPath` are already present | T1-T3 | COVERED | Reuse and verify; do not recreate the legacy heavy hook. |
| RESEARCH | CI | Format check precedes existing Ubuntu gates | T1,T3 | COVERED | Existing gate order retained. |
| RESEARCH | PACKAGE-AUDIT | Official installed/locked artifacts; no acquisition legitimacy checkpoint | T1 | COVERED | Preconditions fail closed if artifacts disappear. |
| RESEARCH | FINAL-GATES | One frozen non-browser sequence and reverse-apply evidence | T3 | COVERED | No Playwright or release-exit claim. |
| RESEARCH | SECURITY-DOMAIN | Research sentence claimed security enforcement was enabled | — | EXCLUDED | Live config explicitly sets it false, and the locked tooling boundary disables that capability; no threat model is planned. |
| CONTEXT | D-01 | Fresh standalone formatting milestone; deprecated R01 is not evidence | T1,T3 | COVERED | Current active branch only; fresh R01 remains outside phase. |
| CONTEXT | D-02 | Absolute nested quarantine, never consumed | T1-T3 | COVERED | Structural rules only; every repo-wide command excludes all `.planning/**`. |
| CONTEXT | D-03 | Native OpenGSD is sole lifecycle; no parallel infrastructure | T1,T3 | COVERED | No new controller, validator, receipt, scanner, cache, graph, or telemetry. |
| CONTEXT | D-04 | Primary checkout; no worktree | T1,T3 | COVERED | One autonomous plan in current checkout. |
| CONTEXT | D-04a | Bounded authorization; no routine confirmation | T1,T3 | COVERED | No checkpoints; bounded in-scope repair only. |
| CONTEXT | D-05 | Root `.gitattributes` LF policy | T1 | COVERED | Exact owner and rules specified. |
| CONTEXT | D-06 | One config/ignore and only format/check commands | T1 | COVERED | No second entrypoint. |
| CONTEXT | D-07 | Preserve style; supported Tailwind v4 integration | T1 | COVERED | Existing style keys retained; live stylesheet configured. |
| CONTEXT | D-08 | Exact stable pins after official research | T1 | COVERED | Exact retained versions/integrities verified; no install. |
| CONTEXT | D-09 | Complete allowed surface, all planning excluded | T2 | COVERED | Root canonical command owns selection. |
| CONTEXT | D-10 | Exactly one formatter-only baseline commit | T2-T3 | COVERED | Fixed-point staged result committed once and inspected. |
| CONTEXT | D-11 | Second write no diff; complete check passes | T2-T3 | COVERED | Precommit and frozen-revision proof. |
| CONTEXT | D-12 | Mechanical commit separate; counts are not gates | T2-T3 | COVERED | Separation is content-based. |
| CONTEXT | D-13 | Direct supported runtime or existing wrapper; no PATH mutation | T1,T3 | COVERED | Route selected and recorded. |
| CONTEXT | D-14 | Retain wrapper; hook resolves direct npm first | T1,T3 | COVERED | Existing files reused. |
| CONTEXT | D-15 | Fast hook; full suite only on frozen candidate | T1-T3 | COVERED | Bootstrap/full hook paths plus one final suite. |
| CONTEXT | D-16 | Ubuntu check before existing gates | T1,T3 | COVERED | CI step/order enforced. |
| CONTEXT | D-17 | Complete evidence, no browser/product UAT | T3 | COVERED | Exact non-browser sequence and SUMMARY evidence. |
| CONTEXT | D-18 | Verification does not prove release | T3 | COVERED | Release-exit actions explicitly prohibited in this plan. |
| CONTEXT | D-19 | Fresh R01 only after release exit | T3 | COVERED | No R01 action or deprecated input. |
| CONTEXT | Deferred | Product work, dormant seeds, browser/UAT, release-exit R01 | — | EXCLUDED | Explicit deferred/out-of-scope items do not appear in tasks. |

<verification>
1. Validate PLAN frontmatter/structure with native `frontmatter.validate --schema plan` and `verify.plan-structure` before execution.
2. Task 1 proves the retained package identity/config/runtime/hook/CI tracer without a repository-wide write or package acquisition.
3. Task 2 proves the staged mechanical diff is non-empty, policy-free, formatter-only, and at a repeated-write fixed point before the single baseline commit.
4. Task 3 binds the canonical check, no-diff write, lint, typecheck, full unit suite, build, reverse-apply proof, and clean allowed worktree to one exact implementation revision.
5. Verify the implementation creates no application symbols and performs no `.planning/**` formatter traversal, product/browser test, worktree operation, or release-exit action.
</verification>

<success_criteria>
- All 14 Phase 1 requirement IDs are satisfied by the single plan and appear exactly once in its frontmatter.
- Every locked decision D-01 through D-19 is implemented or enforced by a cited task; deferred ideas remain absent.
- The existing installed packages, hook, wrapper, Git configuration, and native lifecycle are reused rather than duplicated.
- The only repository-wide formatting result is one isolated, idempotent, formatter-only commit outside `.planning/**`.
- One frozen implementation revision passes the complete required non-browser sequence and has clean rollback evidence without claiming release completion.
</success_criteria>

<output>
Create `.planning/phases/01-repository-formatting-baseline/01-01-SUMMARY.md` when done.
</output>
