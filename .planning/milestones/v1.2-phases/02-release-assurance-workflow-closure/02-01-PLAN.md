---
phase: 02-release-assurance-workflow-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/PROJECT.md
  - tests/unit/quick-metronome-session.test.ts
  - .githooks/pre-commit
autonomous: true
requirements:
  - ID-01
  - ID-02
  - ID-03
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - PDF-01
  - AUTH-01
  - FLOW-01
  - SCOPE-01
must_haves:
  truths:
    - "Active PROJECT, STATE, ROADMAP, and AGENTS authority consistently identifies v1.1 as shipped, v1.2 as the active assurance milestone, Native OpenGSD as sole lifecycle owner, product/R01 scope as dormant, and ordinary Native verification as the authorization endpoint."
    - "When both secure Web Crypto APIs exist, quick-recording creation uses one crypto.randomUUID() result with the recording_ prefix, never calls getRandomValues(), and keeps artifactRef.artifactId equal to recording.id; the existing 16-byte fallback and no-secure-source failure remain green without changing production session.ts."
    - "The pre-commit hook authorizes a direct route only from the exact captured Node and npm candidates when both stable versions satisfy the simple lower bounds extracted from the staged root package.json; every unproven pair uses the existing PowerShell fallback or fails non-zero when no route exists."
    - "The hook retains its staged-index bootstrap, whitespace check, .planning/** exclusion, snapshot cleanup, exit propagation, exact PowerShell argument forwarding, node_modules/.bin prepend, and fast format:check boundary."
    - "Both PDF worktree blobs equal their HEAD blobs, retain the committed 598/936-byte and 415/713 startxref invariants, and have no staged or unstaged path diff."
    - "The integrated revision changes only the four approved owners, passes proportional revision-bound gates, reaches ordinary Native OpenGSD verification, and performs no shipping or release-exit action."
  artifacts:
    - path: AGENTS.md
      provides: "Current Native OpenGSD routing authority with shipped/current milestone facts and a verification stop"
      contains: "Native OpenGSD"
    - path: .planning/PROJECT.md
      provides: "Precise secure quick-recording recording-ID terminology and bounded assurance scope"
      contains: "secure quick-recording"
    - path: tests/unit/quick-metronome-session.test.ts
      provides: "Deterministic randomUUID precedence characterization beside the existing fallback and fail-closed cases"
      contains: "prefers randomUUID when both secure ID APIs are available"
    - path: src/lib/quick-metronome/session.ts
      provides: "Existing secure recording-ID implementation retained byte-for-byte"
      contains: "Secure random number generation is unavailable"
    - path: .githooks/pre-commit
      provides: "Engine-aware exact-candidate selector inside the existing staged-index gate"
      contains: "git show :package.json"
    - path: scripts/npm-local.ps1
      provides: "Existing repository-local Node/Corepack/npm fallback retained byte-for-byte"
      contains: "npm@11.17.0"
    - path: test-fixtures/sheets/real-sheet.pdf
      provides: "Immutable 598-byte committed PDF fixture with startxref 415"
    - path: test-fixtures/sheets/two-page-sheet.pdf
      provides: "Immutable 936-byte committed PDF fixture with startxref 713"
    - path: .planning/phases/02-release-assurance-workflow-closure/02-01-SUMMARY.md
      provides: "Native executor evidence bound to the implementation revision"
    - path: .planning/phases/02-release-assurance-workflow-closure/02-VERIFICATION.md
      provides: "Ordinary Native OpenGSD verification result and authorization endpoint"
  key_links:
    - from: tests/unit/quick-metronome-session.test.ts
      to: src/lib/quick-metronome/session.ts
      via: "the existing createQuickRecording import exercises the primary, fallback, and unavailable Web Crypto paths"
      pattern: "createQuickRecording"
    - from: .githooks/pre-commit
      to: package.json
      via: "git show :package.json reads the staged engines.node and engines.npm lower bounds before route selection"
      pattern: "git show :package\\.json"
    - from: .githooks/pre-commit
      to: scripts/npm-local.ps1
      via: "ineligible or unprovable direct candidates preserve the existing powershell-then-pwsh -File fallback"
      pattern: "npm_local_script"
    - from: .githooks/pre-commit
      to: package.json
      via: "the selected exact npm path or unchanged fallback runs the existing staged-snapshot format:check script"
      pattern: "run_npm.*format:check"
    - from: test-fixtures/sheets/real-sheet.pdf
      to: HEAD:test-fixtures/sheets/real-sheet.pdf
      via: "git hash-object --no-filters compared with git rev-parse HEAD:<path>"
      pattern: "hash-object --no-filters"
    - from: .planning/phases/02-release-assurance-workflow-closure/02-01-SUMMARY.md
      to: .planning/phases/02-release-assurance-workflow-closure/02-VERIFICATION.md
      via: "native execute-phase hands the committed implementation and evidence to the ordinary verifier"
      pattern: "Native OpenGSD verification"
---

<objective>
Close the Phase 2 release-assurance loop across the existing authority, secure recording-ID, pre-commit, binary-fixture, and Native lifecycle owners.

Purpose: Make the shipped repository safe to operate by correcting only the confirmed direct-runtime eligibility defect, characterizing the retained Web Crypto contract, proving immutable fixture/scope integrity, and ending at Native verification.
Output: Reconciled active authority, one focused secure-ID precedence test, an engine-aware exact-candidate selector in the tracked hook, ephemeral route-matrix evidence, immutable PDF proof, and revision-bound Native verification evidence.
</objective>

<execution_context>
@C:/Users/wsuto/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wsuto/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@skills/metronome-policy/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/02-release-assurance-workflow-closure/02-RESEARCH.md
@.planning/research/SUMMARY.md
@.planning/milestones/v1.1-phases/01-repository-formatting-baseline/01-01-SUMMARY.md
@package.json
@package-lock.json
@.githooks/pre-commit
@scripts/npm-local.ps1
@src/lib/quick-metronome/session.ts
@tests/unit/quick-metronome-session.test.ts
@.gitattributes
@test-fixtures/sheets/real-sheet.pdf
@test-fixtures/sheets/two-page-sheet.pdf

<planning_notes>
- No CONTEXT.md exists. The owner explicitly approved direct planning from PROJECT, REQUIREMENTS, ROADMAP, and completed research; no unrecorded design preference is inferred.
- `workflow.specless_probe_fallback=false` is the visible recorded choice. This plan generates no probe-derived predicates for SPEC-absent sections and adds no validation layer; every check below comes from the phase requirements or completed research.
- The live config sets `security_enforcement=false`, `workflow.tdd_mode=false`, `workflow.nyquist_validation=false`, and product-domain capabilities off for this tooling-only milestone. No threat model, dedicated TDD plan, Nyquist scaffold, UI/API/schema/AI gate, or post-plan gap layer is emitted.
- The compact reuse decision is binding: reuse `session.ts`, its existing unit owner, `.githooks/pre-commit`, `scripts/npm-local.ps1`, Git object identity, and Native OpenGSD. A production secure-ID edit, package/lock change, wrapper change, PDF-byte edit, new lifecycle mechanism, or parallel owner is a scope-deviation stop.
- `.planning/deprecated/**` remains an absolute quarantine. No task reads, searches, indexes, hashes, formats, cites, or transforms it.
- Tracer mode is active. Task 1 is the thinnest production-quality path through every durable owner this phase modifies; Task 2 expands selector edge coverage without adding a tracked harness; Task 3 proves the integrated revision and hands it to the ordinary verifier.
</planning_notes>

<interfaces>
- `createQuickRecording({ artifact, session, settings, createdAt? })` from `src/lib/quick-metronome/session.ts` returns a `QuickRecording`; its internal `createId("recording")` already orders randomUUID, 16 secure bytes, then the existing error.
- `.githooks/pre-commit` exposes one internal `run_npm "$@"` seam. The route selector must set the exact captured `npm_cmd` or the existing `power_shell`; everything after that seam retains the staged-snapshot behavior.
- `scripts/npm-local.ps1` accepts the forwarded npm argument array after `-File` and supplies process-local Node 24.17.0/Corepack/npm 11.17.0. Its bytes and public invocation contract do not change.
</interfaces>
</context>

## Artifacts this phase produces

- New tracked paths: none. The phase modifies only `AGENTS.md`, `.planning/PROJECT.md`, `tests/unit/quick-metronome-session.test.ts`, and `.githooks/pre-commit`.
- New test case: `prefers randomUUID when both secure ID APIs are available` in the existing quick-metronome unit owner.
- New hook-local helpers: `extract_engine_minimum`, `normalize_stable_version`, and `version_at_least`; new route state captures the exact `node_cmd` while retaining the existing `npm_cmd` and `power_shell` seams. No exported application symbol is added.
- Ephemeral-only paths: `C:/tmp/metronome-phase-02-hook-matrix/**` for the route runner, fake candidates, minimal Git repository, provenance logs, and result JSON. The directory is never staged, remains only until Task 3 binds its evidence to the frozen hook blob, and is then removed by Task 3's guarded cleanup.
- Native lifecycle artifacts: `02-01-SUMMARY.md` from execution and `02-VERIFICATION.md` from the ordinary Native verifier. No project-owned receipt, controller, sidecar, checksum file, or final-review-specific verification artifact is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile authority and prove one secure-ID-to-hook route end to end</name>
  <precondition>The tracked hook is mode 100755 with core.hooksPath=.githooks, scripts/npm-local.ps1 exists unchanged, Git for Windows Bash can create and remove only its temporary .git/pre-commit-index.* directory, and C:/tmp/metronome-phase-02-hook-matrix does not already exist.</precondition>
  <reversibility rating="costly">The hook route contract and active repository authority affect every later commit and native dispatch, but undo requires only coordinated file reverts and no data migration or published product-contract break.</reversibility>
  <files>AGENTS.md, .planning/PROJECT.md, tests/unit/quick-metronome-session.test.ts, .githooks/pre-commit</files>
  <read_first>
    - `AGENTS.md` Repository workflow, Reuse contract, Release exit, and Git hook sections
    - `.planning/PROJECT.md` Current Milestone, Constraints, Key Decisions, and Evolution
    - `.planning/STATE.md` Current Position, Accumulated Context, and Deferred Items
    - `.planning/ROADMAP.md` Phase 2 Goal, Requirements, Success Criteria, and plan boundary
    - `.planning/REQUIREMENTS.md` ID-01 through ID-03, HOOK-01 through HOOK-03, AUTH-01, FLOW-01, SCOPE-01, Future Requirements, and Out of Scope
    - `.planning/phases/02-release-assurance-workflow-closure/02-RESEARCH.md` Summary, Architectural Responsibility Map, Compact Reuse Decision, Recommended Project Structure, Patterns 1-2, and Pitfalls 1-4
    - `src/lib/quick-metronome/session.ts` `createId` branch order and exported `createQuickRecording` contract
    - `tests/unit/quick-metronome-session.test.ts` quick-recording metadata describe block, existing fallback/failure tests, and `vi.unstubAllGlobals()` cleanup
    - `.githooks/pre-commit` selector, `run_npm`, bootstrap, snapshot, cleanup, and formatting flow
    - `scripts/npm-local.ps1` argument forwarding, process-local PATH, and pinned runtime
    - `package.json` current root `engines.node`, `engines.npm`, and `format:check`
  </read_first>
  <behavior>
    - "Authority: active routing says the formatting milestone shipped, the assurance milestone is current, dormant product/R01 work remains excluded, and authorization stops after ordinary Native verification."
    - "Secure ID: both APIs present -> recording_<randomUUID>, artifactRef.artifactId equals recording.id, randomUUID called once, getRandomValues never called."
    - "Tracer direct route: staged >=24.0.0 / >=11.17.0 plus exact v24.0.0 / 11.17.0 candidates -> the same captured npm candidate is probed and invoked."
    - "Tracer fallback route: a candidate pair below either staged minimum -> the existing PowerShell route receives the original npm argument array."
  </behavior>
  <action>
Execute the following substeps in order; do not begin the next substep until the preceding checks pass.

1. Capture the implementation base with `git log -1 --format=%H -- .planning/phases/02-release-assurance-workflow-closure/02-01-PLAN.md` and record it for the SUMMARY. Reconcile only current authority: replace the stale active-release/shipping clauses in `AGENTS.md` with the approved shipped-formatting/current-assurance/native-verification-stop facts from AUTH-01 and SCOPE-01, while preserving generally valid repository workflow, hook, and release-truth rules. In `.planning/PROJECT.md`, change the single target-outcome phrase that incorrectly calls the quick-recording value a session identifier so it accurately names the recording-ID contract. Do not rewrite milestone archives, hand-edit STATE or ROADMAP lifecycle fields, create a lifecycle rule, or touch the quarantined history. Run native `smart-entry --json` read-only and stop on a genuine contradiction rather than routing to release work.

2. Add the named deterministic test immediately beside the existing secure-byte fallback and unavailable-crypto cases. Stub both APIs with `vi.fn`, call the existing `createQuickRecording`, assert the exact `recording_`-prefixed UUID and recording/artifact identity, assert one primary call and zero byte-fallback calls, and rely on the existing `afterEach` global restoration. Run the focused owner after the test. Leave `src/lib/quick-metronome/session.ts` byte-identical; any production failure is a bounded diagnosis stop, not authorization to redesign the ID path.

3. Before changing the selector, create the phase-owned temporary harness at the exact preconditioned path with a minimal committed `.prettierignore`, staged root manifest, copied current hook, distinct fake Node/npm/PowerShell candidates, provenance logs, and a runner that emits structured case results. Add the exact-minimum direct case and the too-old-Node fallback case first; confirm the latter exposes the current presence-only defect. Then edit only the route-eligibility region of `.githooks/pre-commit`. Add `extract_engine_minimum` to accept exactly one staged root `engines.<name>` string shaped as a stable `>=MAJOR.MINOR.PATCH`; add `normalize_stable_version` to remove CR/LF and only Node's single leading `v`, accept a full stable triplet with optional build metadata, and reject missing, failed, malformed, or prerelease output; add `version_at_least` to compare all three numeric components. Read the manifest with guarded `git show :package.json`, resolve absolute Node and `npm`-then-`npm.cmd` candidates before the existing PATH prepend, probe those exact paths under guarded conditionals, and store the exact npm path in `npm_cmd` only when both normalized stable versions satisfy the staged minima. Otherwise preserve the unchanged `scripts/npm-local.ps1` existence check, `powershell` then `pwsh` order, exact flags and `"$@"` forwarding, or the existing actionable non-zero failure.

Keep `set -euo pipefail`, repository/Git-directory resolution, cleanup traps and guards, staged whitespace check, HEAD bootstrap guard, `run_npm` interface, `node_modules/.bin` prepend position, `mktemp` prefix, NUL-delimited index export, `.planning/**` exclusion, snapshot prefix, format command, and exit propagation structurally unchanged per HOOK-03. Do not use the transitive semver package, alter any manifest/lock/wrapper byte, refactor cleanup, print secrets, mutate global/user PATH, or add a tracked harness. Format the new TypeScript test through the existing formatter route, stage exactly the four task-owned paths, run the two tracer harness cases, the focused unit owner, Git Bash syntax checking, and the real tracked hook against its staged snapshot. Commit only after all tracer checks pass.
  </action>
  <acceptance_criteria>
    - AUTH-01 active authority is coherent without archive rewrites or a custom lifecycle mechanism, and read-only native routing remains inside Phase 2 rather than release work.
    - ID-01 has a deterministic primary-branch test; ID-02 and ID-03 remain covered by the adjacent existing tests; `src/lib/quick-metronome/session.ts` has no byte diff from the implementation base.
    - HOOK-01 authorizes the exact minimum direct pair from staged simple lower bounds, and HOOK-02 routes the too-old tracer pair through the existing fallback with original arguments.
    - HOOK-03 bootstrap, staged snapshot, exclusion, cleanup, failure, whitespace, and format-only behavior remains intact.
    - The implementation diff is limited to the four declared paths; package.json, package-lock.json, scripts/npm-local.ps1, production secure-ID code, and both PDFs remain unchanged.
    - The temporary harness is outside the repository and untracked; no durable wrapper, controller, dependency, or test subsystem is created.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
$agents = Get-Content -Raw -LiteralPath AGENTS.md
$project = Get-Content -Raw -LiteralPath .planning/PROJECT.md
if ($agents -match "release exit is active|release exit is still pending") { throw "Stale active release authority remains" }
if ($agents -notmatch "v1\.1[\s\S]{0,160}shipped" -or $agents -notmatch "v1\.2[\s\S]{0,160}(active|current)" -or $agents -notmatch "authorization[\s\S]{0,160}(stops|ends)[\s\S]{0,160}(Native OpenGSD|native) verification") { throw "Active authority facts are incomplete" }
if ($project.Contains("secure quick-recording session-ID contract")) { throw "PROJECT still misnames the recording ID" }
$base = (git log -1 --format=%H -- .planning/phases/02-release-assurance-workflow-closure/02-01-PLAN.md).Trim()
git diff --exit-code "$base" -- src/lib/quick-metronome/session.ts package.json package-lock.json scripts/npm-local.ps1 test-fixtures/sheets/real-sheet.pdf test-fixtures/sheets/two-page-sheet.pdf
if ($LASTEXITCODE -ne 0) { throw "A preserved owner changed" }
& .\scripts\npm-local.ps1 --% run test:unit -- tests/unit/quick-metronome-session.test.ts
if ($LASTEXITCODE -ne 0) { throw "Focused secure-ID characterization failed" }
& 'C:\Program Files\Git\bin\bash.exe' -lc 'cd /c/Users/wsuto/metronome && bash -n .githooks/pre-commit'
if ($LASTEXITCODE -ne 0) { throw "Hook syntax check failed" }
$matrixRoot = 'C:\tmp\metronome-phase-02-hook-matrix'
& (Join-Path $matrixRoot 'run-matrix.ps1') -Cases @('exact-minimum','too-old-node') -HookPath (Resolve-Path .githooks/pre-commit)
if ($LASTEXITCODE -ne 0) { throw "Tracer route cases failed" }
& 'C:\Program Files\Git\bin\bash.exe' -lc 'cd /c/Users/wsuto/metronome && ./.githooks/pre-commit'
if ($LASTEXITCODE -ne 0) { throw "Real staged-snapshot hook integration failed" }
$expected = @('.githooks/pre-commit','.planning/PROJECT.md','AGENTS.md','tests/unit/quick-metronome-session.test.ts') | Sort-Object
$actual = @(git diff --cached --name-only --) | Sort-Object
if (@(Compare-Object -ReferenceObject $expected -DifferenceObject $actual).Count -ne 0) { throw "Tracer staged paths differ from the four approved owners" }
git diff --cached --check
if ($LASTEXITCODE -ne 0) { throw "Tracer staged whitespace check failed" }
    </automated>
  </verify>
  <done>The current authority, secure-ID contract, exact-minimum direct route, and conservative fallback form one committed end-to-end slice through the existing owners, with production ID code and fallback wrapper unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the controlled route matrix and preserve the complete hook boundary</name>
  <precondition>C:/tmp/metronome-phase-02-hook-matrix exists from Task 1, resolves exactly beneath C:/tmp, is not a Git worktree or tracked repository path, and the committed hook still has mode 100755 with core.hooksPath=.githooks.</precondition>
  <reversibility rating="reversible">The only durable correction remains inside one hook selector region; the matrix and sentinels are disposable temporary evidence.</reversibility>
  <files>.githooks/pre-commit</files>
  <read_first>
    - `.planning/phases/02-release-assurance-workflow-closure/02-RESEARCH.md` Required route matrix, Pattern 3, Anti-Patterns, Pitfalls 2-6, Code Examples, Open Question 1, and Environment Availability
    - `.planning/REQUIREMENTS.md` HOOK-01 through HOOK-03 and SCOPE-01
    - `AGENTS.md` Git hook and reuse/exact-evidence rules after Task 1 reconciliation
    - `.githooks/pre-commit` committed Task 1 selector and all unchanged snapshot/cleanup regions
    - `scripts/npm-local.ps1` exact cross-shell argument and exit behavior; read only, do not modify
    - `package.json` staged simple engine minima and `format:check` contract; read only, do not modify
    - `.gitattributes` and `.prettierignore` binary/planning and staged-format boundaries
  </read_first>
  <behavior>
    - "exact-minimum, higher-same-major, and higher-major pairs -> exact direct npm candidate, with probe path equal to invocation path."
    - "missing-node-fallback -> Node is absent while a valid npm candidate exists, so the existing PowerShell fallback receives the unchanged arguments."
    - "npm-cmd-only-direct -> npm is absent while a compatible npm.cmd exists, so that exact npm.cmd path is selected, probed, and invoked directly."
    - "pwsh-only-fallback -> powershell is absent while pwsh exists, so the existing fallback runs through pwsh with unchanged arguments."
    - "too-old-node, too-old-npm, malformed-candidate-version, prerelease-candidate-version, failed-candidate-probe, missing-direct-command, missing-engine-declaration, duplicate-engine-declaration, compound-engine-range, and unsupported-engine-range -> existing fallback route."
    - "fallback-success -> powershell before pwsh, exact -File wrapper invocation, unchanged npm argument vector, and propagated zero exit."
    - "no-route -> actionable non-zero selector failure after required Git Bash utilities remain available."
    - "competing npm and npm.cmd candidates -> one priority candidate is captured, probed, and invoked without mixing evidence."
  </behavior>
  <action>
Expand the existing temporary runner before changing durable code. Each case must initialize a fresh minimal Git repository beneath `C:/tmp/metronome-phase-02-hook-matrix/cases/<case>/repo`, place executable sentinels beneath the corresponding `cases/<case>/bin`, commit the bootstrap files, stage the case-specific raw root package.json, copy the current tracked hook, preserve Git Bash system paths for `git`, `mktemp`, `sed`/`awk`, `rm`, and `cygpath`, and prepend only those distinguishable candidate sentinels. Normalize executable provenance to canonical Git-Bash absolute paths rooted at `/c/tmp/metronome-phase-02-hook-matrix/cases/<case>/...`. Emit one structured result per case with `case`, ordered `stagedNodeRanges` and `stagedNpmRanges` arrays, `candidateNodeOutput`, `candidateNpmOutput`, `alternateNpmCmdOutput`, `route`, `selectedNpmForm`, exact `nodeProbePath`, exact `npmProbePath`, exact `npmInvokePath`, `fallbackHost`, normalized `forwardedArgs` array, exact `exitCode`, exact `routeProvenance`, actionable `message`, `snapshotCleaned`, ordered `snapshotResidue`, and `hookHash`. Resolve and eligibility-probe every available exact Node and priority npm candidate under the simple staged range cases and record its exact sentinel path even when the pair is later rejected; use `ABSENT` only when that candidate does not exist and `NOT_PROBED` only when an unsupported staged declaration rejects eligibility before candidate execution. `selectedNpmForm` records the priority captured form (`npm`, `npm.cmd`, or `NONE`). A direct result must use the same exact path for `npmProbePath` and `npmInvokePath`; fallback and failure results must use `NONE` for `npmInvokePath` because no direct candidate ran. Use `PROBE_FAILED_7` for the deliberately failing npm version probe, `NONE` for an unselected route endpoint, and normalize only the dynamic snapshot argument to `SNAPSHOT_ROOT` so forwarding can be compared exactly. Encode `routeProvenance` exactly as `direct|<selectedNpmForm>|<npmInvokePath>`, `fallback|<fallbackHost>|<case-local-wrapper-path>`, or `failure|no-supported-route`; every result must prove its staged `.git/pre-commit-index.*` snapshot was removed and name no residue.

Create and run these exact independent cases: `exact-minimum` uses staged Node `>=24.0.0`, staged npm `>=11.17.0`, Node `v24.0.0`, and npm `11.17.0`; `higher-same-major` uses the same ranges with Node `v24.17.0` and npm `11.17.1`; `higher-major` uses the same ranges with Node `v25.0.0` and npm `12.0.0`; `missing-node-fallback` has no Node candidate but has npm `11.17.0` and the existing `powershell` fallback; `npm-cmd-only-direct` has Node `v24.17.0`, no npm candidate, and npm.cmd `11.17.1`; `pwsh-only-fallback` uses Node `v23.99.99`, npm `11.16.9`, no `powershell`, and an available `pwsh`; `too-old-node` uses Node `v23.99.99` and npm `11.17.0`; `too-old-npm` uses Node `v24.17.0` and npm `11.16.9`; `malformed-candidate-version` uses Node `v24.17.0` and npm output `npm-11.17.0`; `prerelease-candidate-version` uses Node `v25.0.0-rc.1` and npm `11.17.0`; `failed-candidate-probe` uses Node `v24.17.0` and an npm version command that exits 7; and `missing-direct-command` uses Node `v24.17.0` with no npm or npm.cmd candidate. Unless stated otherwise in this list, these cases stage the simple `>=24.0.0` / `>=11.17.0` ranges, have no alternate npm.cmd candidate, and expose the `powershell` sentinel before `pwsh`.

Exercise each staged-engine declaration failure separately with otherwise compatible candidates `v24.17.0` / `11.17.0`: `missing-engine-declaration` stages no Node range and one npm `>=11.17.0`; `duplicate-engine-declaration` stages one Node `>=24.0.0` and two raw npm declarations both equal to `>=11.17.0`; `compound-engine-range` stages Node `>=24.0.0 <25.0.0` and npm `>=11.17.0`; and `unsupported-engine-range` stages Node `>=24.0.0` and npm `^11.17.0`. `fallback-success` stages the simple ranges with Node `v23.99.99`, npm `11.16.9`, and both fallback sentinels available so `powershell` wins. `no-route` stages the simple ranges, has neither direct candidate nor PowerShell host while retaining the wrapper fixture and required Git utilities, and must fail non-zero. `competing-npm-forms` stages the simple ranges with Node `v24.17.0`, npm `11.17.1`, and npm.cmd `12.0.0`, proving the existing priority candidate is the one probed and invoked.

The three original compatible cases, `npm-cmd-only-direct`, and `competing-npm-forms` must select direct and show the same exact npm path at probe and invocation. Every other unproven case must select fallback, except `no-route`, which must expose route `failure`, exact exit `1`, fallback host `NONE`, empty forwarded arguments, and the selector's actionable message rather than fail earlier on a missing Git utility. Every ordinary fallback result must show `powershell`; `pwsh-only-fallback` alone must show `pwsh`. Every direct and fallback result must preserve the exact normalized argument array `--prefix`, `SNAPSHOT_ROOT`, `run`, `format:check` and exit `0`. For every case, independently assert the exact Node probe path, `ABSENT`, or declaration-rejection `NOT_PROBED`; selected npm form; exact npm probe path, `ABSENT`, or declaration-rejection `NOT_PROBED`; direct probe/invocation equality or explicit non-direct `NONE`; fallback host or `NONE`; forwarded vector; exact exit; route provenance; hook hash; and staged snapshot cleanup. Include one real-checkout integration using Git for Windows Bash with direct candidates withheld and the actual unchanged `scripts/npm-local.ps1`; it must reach `staged-index npm run format:check`, leave no `.git/pre-commit-index.*` child, and keep `.planning/**` outside the snapshot.

If a case fails, edit only `extract_engine_minimum`, `normalize_stable_version`, `version_at_least`, or the adjacent exact-candidate selection statements in `.githooks/pre-commit`, then rerun every case. Do not generalize compound SemVer grammar, use `sort -V`, promote transitive semver, change candidate priority, redesign PowerShell fallback, touch cleanup/snapshot/bootstrap/format sections, or create a tracked runner. When all cases pass, keep `matrix-results.json` and its provenance logs only at `C:/tmp/metronome-phase-02-hook-matrix` for Task 3 to bind to the frozen `HEAD:.githooks/pre-commit` blob; prove the exact temporary root and clean repository status, but do not remove the root yet. Task 3 owns the single guarded cleanup after provenance is captured. Commit a hook change only if the matrix required an in-scope correction beyond Task 1; otherwise complete this task with evidence and no empty commit.
  </action>
  <acceptance_criteria>
    - HOOK-01 passes exact-minimum, higher-same-major, higher-major, npm-cmd-only-direct, and competing-npm-forms direct cases with the stated candidate tuples, selected npm form, exact Node/npm probe provenance, numeric major/minor/patch comparison, and identical direct npm probe/invocation paths.
    - HOOK-02 independently passes missing-node-fallback, pwsh-only-fallback, too-old Node, too-old npm, malformed candidate, prerelease candidate, failed probe, missing direct command, missing declaration, duplicate declaration, compound range, unsupported range, fallback-success, and no-route cases with exact structured inputs, selected fallback host or explicit none, unchanged argument vectors, exact exit status, route provenance, and staged snapshot cleanup.
    - HOOK-03 retains bootstrap, staged whitespace, NUL snapshot export, `.planning/**` exclusion, cleanup guards, `node_modules/.bin` position, original argument forwarding, exit propagation, and the sole `format:check` boundary.
    - The real fallback integration uses the unchanged repository wrapper and completes the staged-snapshot format check under Git for Windows Bash.
    - The tested hook hash and complete result table remain only in the exact temporary root for Task 3 provenance binding, while no package/lock/wrapper/production/PDF byte changes occur and no harness path becomes a repository artifact.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
$matrixRoot = 'C:\tmp\metronome-phase-02-hook-matrix'
$resolvedRoot = [IO.Path]::GetFullPath($matrixRoot).TrimEnd('\')
if ($resolvedRoot -ne 'C:\tmp\metronome-phase-02-hook-matrix') { throw "Unexpected matrix root" }
& (Join-Path $matrixRoot 'run-matrix.ps1') -Cases @('exact-minimum','higher-same-major','higher-major','missing-node-fallback','npm-cmd-only-direct','pwsh-only-fallback','too-old-node','too-old-npm','malformed-candidate-version','prerelease-candidate-version','failed-candidate-probe','missing-direct-command','missing-engine-declaration','duplicate-engine-declaration','compound-engine-range','unsupported-engine-range','fallback-success','no-route','competing-npm-forms') -HookPath (Resolve-Path .githooks/pre-commit) -RealRepository (Resolve-Path .)
if ($LASTEXITCODE -ne 0) { throw "Complete hook route matrix failed" }
$results = @(Get-Content -Raw -LiteralPath (Join-Path $matrixRoot 'matrix-results.json') | ConvertFrom-Json)
$expectedRoutes = [ordered]@{
    'exact-minimum'='direct'; 'higher-same-major'='direct'; 'higher-major'='direct'; 'missing-node-fallback'='fallback'; 'npm-cmd-only-direct'='direct'; 'pwsh-only-fallback'='fallback'; 'too-old-node'='fallback'; 'too-old-npm'='fallback'; 'malformed-candidate-version'='fallback'; 'prerelease-candidate-version'='fallback'; 'failed-candidate-probe'='fallback'; 'missing-direct-command'='fallback'; 'missing-engine-declaration'='fallback'; 'duplicate-engine-declaration'='fallback'; 'compound-engine-range'='fallback'; 'unsupported-engine-range'='fallback'; 'fallback-success'='fallback'; 'no-route'='failure'; 'competing-npm-forms'='direct'
}
$expectedInputs = [ordered]@{
    'exact-minimum'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.0.0'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'higher-same-major'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='11.17.1'; NpmCmd='ABSENT' }
    'higher-major'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v25.0.0'; Npm='12.0.0'; NpmCmd='ABSENT' }
    'missing-node-fallback'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='ABSENT'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'npm-cmd-only-direct'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='ABSENT'; NpmCmd='11.17.1' }
    'pwsh-only-fallback'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v23.99.99'; Npm='11.16.9'; NpmCmd='ABSENT' }
    'too-old-node'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v23.99.99'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'too-old-npm'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='11.16.9'; NpmCmd='ABSENT' }
    'malformed-candidate-version'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='npm-11.17.0'; NpmCmd='ABSENT' }
    'prerelease-candidate-version'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v25.0.0-rc.1'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'failed-candidate-probe'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='PROBE_FAILED_7'; NpmCmd='ABSENT' }
    'missing-direct-command'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='ABSENT'; NpmCmd='ABSENT' }
    'missing-engine-declaration'=@{ NodeRanges=@(); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'duplicate-engine-declaration'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0','>=11.17.0'); Node='v24.17.0'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'compound-engine-range'=@{ NodeRanges=@('>=24.0.0 <25.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'unsupported-engine-range'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('^11.17.0'); Node='v24.17.0'; Npm='11.17.0'; NpmCmd='ABSENT' }
    'fallback-success'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v23.99.99'; Npm='11.16.9'; NpmCmd='ABSENT' }
    'no-route'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='ABSENT'; Npm='ABSENT'; NpmCmd='ABSENT' }
    'competing-npm-forms'=@{ NodeRanges=@('>=24.0.0'); NpmRanges=@('>=11.17.0'); Node='v24.17.0'; Npm='11.17.1'; NpmCmd='12.0.0' }
}
function Assert-StringArrayEqual([object[]]$Actual, [object[]]$Expected, [string]$Label) {
  $actualText = @($Actual | ForEach-Object { [string]$_ }) -join [char]31
  $expectedText = @($Expected | ForEach-Object { [string]$_ }) -join [char]31
  if ($actualText -ne $expectedText) { throw "Structured input mismatch: $Label" }
}
$expectedForwardedArgs = @('--prefix','SNAPSHOT_ROOT','run','format:check')
$declarationRejectedCases = @('missing-engine-declaration','duplicate-engine-declaration','compound-engine-range','unsupported-engine-range')
function Get-CasePosixPath([string]$CaseName, [string]$RelativePath) {
  return "/c/tmp/metronome-phase-02-hook-matrix/cases/$CaseName/$RelativePath"
}
$observedCaseNames = @($results | ForEach-Object { [string]$_.case }) | Sort-Object
$expectedCaseNames = @($expectedRoutes.Keys) | Sort-Object
if ($results.Count -ne $expectedCaseNames.Count -or @(Compare-Object -ReferenceObject $expectedCaseNames -DifferenceObject $observedCaseNames).Count -ne 0) { throw "Matrix result set is incomplete, duplicated, or unexpected" }
$hookHash = (git hash-object --no-filters -- .githooks/pre-commit).Trim()
foreach ($caseName in $expectedRoutes.Keys) {
    $row = @($results | Where-Object case -eq $caseName)
    if ($row.Count -ne 1 -or $row[0].route -ne $expectedRoutes[$caseName]) { throw "Route mismatch for $caseName" }
    $input = $expectedInputs[$caseName]
    Assert-StringArrayEqual -Actual @($row[0].stagedNodeRanges) -Expected @($input.NodeRanges) -Label "$caseName staged Node ranges"
    Assert-StringArrayEqual -Actual @($row[0].stagedNpmRanges) -Expected @($input.NpmRanges) -Label "$caseName staged npm ranges"
    if ([string]$row[0].candidateNodeOutput -ne $input.Node -or [string]$row[0].candidateNpmOutput -ne $input.Npm -or [string]$row[0].alternateNpmCmdOutput -ne $input.NpmCmd) { throw "Candidate tuple mismatch for $caseName" }
    $expectedNpmForm = if ($input.Npm -ne 'ABSENT') { 'npm' } elseif ($input.NpmCmd -ne 'ABSENT') { 'npm.cmd' } else { 'NONE' }
    $expectedNodeProbePath = if ($declarationRejectedCases -contains $caseName) { 'NOT_PROBED' } elseif ($input.Node -eq 'ABSENT') { 'ABSENT' } else { Get-CasePosixPath -CaseName $caseName -RelativePath 'bin/node' }
    $expectedNpmProbePath = if ($declarationRejectedCases -contains $caseName) { 'NOT_PROBED' } elseif ($expectedNpmForm -eq 'NONE') { 'ABSENT' } else { Get-CasePosixPath -CaseName $caseName -RelativePath "bin/$expectedNpmForm" }
    $expectedFallbackHost = if ($row[0].route -eq 'fallback') { if ($caseName -eq 'pwsh-only-fallback') { 'pwsh' } else { 'powershell' } } else { 'NONE' }
    $expectedNpmInvokePath = if ($row[0].route -eq 'direct') { $expectedNpmProbePath } else { 'NONE' }
    $expectedExitCode = if ($row[0].route -eq 'failure') { 1 } else { 0 }
    $expectedArgs = if ($row[0].route -eq 'failure') { @() } else { $expectedForwardedArgs }
    $expectedRouteProvenance = if ($row[0].route -eq 'direct') {
      "direct|$expectedNpmForm|$expectedNpmInvokePath"
    } elseif ($row[0].route -eq 'fallback') {
      "fallback|$expectedFallbackHost|$(Get-CasePosixPath -CaseName $caseName -RelativePath 'repo/scripts/npm-local.ps1')"
    } else {
      'failure|no-supported-route'
    }
    if ([string]$row[0].nodeProbePath -ne $expectedNodeProbePath) { throw "Node probe provenance mismatch for $caseName" }
    if ([string]$row[0].selectedNpmForm -ne $expectedNpmForm) { throw "Selected npm form mismatch for $caseName" }
    if ([string]$row[0].npmProbePath -ne $expectedNpmProbePath -or [string]$row[0].npmInvokePath -ne $expectedNpmInvokePath) { throw "npm probe/invocation provenance mismatch for $caseName" }
    if ($row[0].route -eq 'direct' -and $row[0].npmProbePath -ne $row[0].npmInvokePath) { throw "Direct npm probe and invocation paths differ for $caseName" }
    if ([string]$row[0].fallbackHost -ne $expectedFallbackHost) { throw "Fallback host mismatch for $caseName" }
    Assert-StringArrayEqual -Actual @($row[0].forwardedArgs) -Expected @($expectedArgs) -Label "$caseName forwarded args"
    if ([int]$row[0].exitCode -ne $expectedExitCode) { throw "Exit status mismatch for $caseName" }
    if ([string]$row[0].routeProvenance -ne $expectedRouteProvenance) { throw "Route provenance mismatch for $caseName" }
    if ([string]$row[0].hookHash -ne $hookHash -or $row[0].snapshotCleaned -ne $true) { throw "Hook hash or staged snapshot cleanup mismatch for $caseName" }
    Assert-StringArrayEqual -Actual @($row[0].snapshotResidue) -Expected @() -Label "$caseName staged snapshot residue"
}
$noRoute = @($results | Where-Object case -eq 'no-route')[0]
if ([int]$noRoute.exitCode -ne 1 -or $noRoute.fallbackHost -ne 'NONE' -or $noRoute.message -notmatch 'supported npm route') { throw "No-route failure is not actionable" }
$fallback = @($results | Where-Object case -eq 'fallback-success')[0]
Assert-StringArrayEqual -Actual @($fallback.forwardedArgs) -Expected $expectedForwardedArgs -Label 'fallback-success forwarded args'
$competing = @($results | Where-Object case -eq 'competing-npm-forms')[0]
if ($competing.selectedNpmForm -ne 'npm' -or $competing.npmProbePath -ne $competing.npmInvokePath) { throw "Competing npm forms mixed candidate evidence" }
& 'C:\Program Files\Git\bin\bash.exe' -lc 'cd /c/Users/wsuto/metronome && bash -n .githooks/pre-commit'
if ($LASTEXITCODE -ne 0) { throw "Final hook syntax check failed" }
if (Get-ChildItem -LiteralPath (git rev-parse --absolute-git-dir) -Filter 'pre-commit-index.*' -Force -ErrorAction SilentlyContinue) { throw "Hook snapshot cleanup left residue" }
git diff --exit-code -- package.json package-lock.json scripts/npm-local.ps1 src/lib/quick-metronome/session.ts .gitattributes test-fixtures/sheets/real-sheet.pdf test-fixtures/sheets/two-page-sheet.pdf
if ($LASTEXITCODE -ne 0) { throw "Route work changed a preserved owner" }
if (-not (Test-Path -LiteralPath (Join-Path $resolvedRoot 'matrix-results.json'))) { throw "Matrix provenance was not retained for Task 3" }
git diff --check
if ($LASTEXITCODE -ne 0) { throw "Hook diff has whitespace errors" }
    </automated>
  </verify>
  <done>All independently named compatible, incompatible, staged-declaration, fallback, and no-route cases pass against one observed hook hash; the real fallback and staged snapshot remain intact, and only the exact external matrix root remains temporarily for Task 3 provenance binding.</done>
</task>

<task type="auto">
  <name>Task 3: Prove immutable scope and hand the frozen revision to Native verification</name>
  <files>.planning/phases/02-release-assurance-workflow-closure/02-01-SUMMARY.md (native executor evidence only; no implementation file is modified)</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` PDF-01, AUTH-01, FLOW-01, SCOPE-01, Future Requirements, and Out of Scope
    - `.planning/ROADMAP.md` Phase 2 Goal and all five Success Criteria
    - `.planning/STATE.md` current phase, decisions, deferred items, and blockers
    - `.planning/PROJECT.md` reconciled target outcomes, constraints, decisions, and release boundary
    - `AGENTS.md` reconciled workflow, reuse, exact-revision, hook, and release-truth rules
    - `.planning/phases/02-release-assurance-workflow-closure/02-RESEARCH.md` Patterns 4-5, Pitfalls 7-8, Focused and Integration Commands, Open Question 2, and Environment Availability
    - `.planning/milestones/v1.1-phases/01-repository-formatting-baseline/01-01-SUMMARY.md` prior exact-revision evidence and established fast-hook/full-candidate pattern
    - `C:/tmp/metronome-phase-02-hook-matrix/matrix-results.json` and its provenance logs retained by Task 2 until this task binds them to the frozen hook blob
    - `test-fixtures/sheets/real-sheet.pdf`, `test-fixtures/sheets/two-page-sheet.pdf`, and `.gitattributes` binary policy
    - The committed `AGENTS.md`, `.planning/PROJECT.md`, test owner, hook, unchanged session owner, wrapper, manifest, and lock from Tasks 1-2
  </read_first>
  <action>
Freeze the implementation revision after Task 2 and record `git rev-parse HEAD`, the plan-commit implementation base, all task commit IDs, and the route-matrix hook hash. Require the committed implementation diff from that base to contain exactly `AGENTS.md`, `.planning/PROJECT.md`, `tests/unit/quick-metronome-session.test.ts`, and `.githooks/pre-commit`; any other implementation path is a scope-deviation stop. Separately prove `package.json`, `package-lock.json`, `scripts/npm-local.ps1`, `src/lib/quick-metronome/session.ts`, `.gitattributes`, and both PDF fixtures have no base-to-candidate diff. Native lifecycle artifacts created after execution are authority outputs, not implementation owners, and must not be folded into this four-path diff.

Before deleting any ephemeral evidence, resolve `C:/tmp/metronome-phase-02-hook-matrix` and require that exact root, parse `matrix-results.json`, require the complete Task 2 case set exactly once, and compare every recorded `hookHash` with both `git rev-parse HEAD:.githooks/pre-commit` and `git hash-object --no-filters -- .githooks/pre-commit`. A missing result, extra result, mixed hook hash, worktree/HEAD mismatch, or later hook edit invalidates the matrix and requires Task 2 to rerun against the replacement hook before continuing. Capture the bound case/input/route table and frozen hook blob ID for `02-01-SUMMARY.md`. If any provenance, authority, PDF, scope, or frozen quality assertion fails, leave that exact temporary root and all evidence intact so the bounded owner-local repair can rerun the same matrix against the replacement hook. Remove only that validated external root after every Task 3 check succeeds, then assert its absence; do not create a permanent harness or custom retry controller.

For each PDF, compare `git hash-object --no-filters -- <path>` with `git rev-parse HEAD:<path>`, require empty staged and unstaged path diffs, and inspect raw bytes without rewriting them. `real-sheet.pdf` must remain 598 bytes with `%PDF-1.4`, one `startxref` value 415 pointing at `xref`, and terminal `%%EOF`; `two-page-sheet.pdf` must retain the same invariants at 936 bytes and offset 713. A mismatch stops the phase; do not copy, restore, normalize, regenerate, or add a checksum sidecar.

Recheck active PROJECT/STATE/ROADMAP/AGENTS facts with deterministic file-specific assertions and run Native `query smart-entry --json` read-only through the repository-local Node executable and installed `gsd-tools.cjs`. Parse the JSON rather than grepping display text: require `signals.current_phase` and `signals.roadmap_next_incomplete_phase` to identify Phase 2, require the summary/recommended action surface to identify the current Phase 2 lifecycle, and reject any v1.1 release, ship, or complete-milestone route. The four authority files must jointly prove the shipped formatting milestone, current assurance phase, dormant product/R01 scope, sole Native lifecycle authority, and post-verification stop. Treat lifecycle status changes made by Native OpenGSD as expected; an unexplained cross-file contradiction is a stop condition. Do not add a project validator, route to shipping, or reinterpret verification as release completion.

Bind evidence to the frozen candidate. The focused secure-ID behavior is included in the final full unit run; the hook route matrix is reusable only when its recorded hook hash equals the frozen `HEAD:.githooks/pre-commit` blob and no later hook edit occurred. Run the existing repository-local route for `format:check`, lint, typecheck, the full unit suite, and build exactly once on this frozen revision because the test and hook inputs changed after the prior milestone evidence. Do not run Playwright, browser UAT, push, PR creation, final-head CI/review, merge, tag, archival, or local-main synchronization. If a gate fails, perform one bounded owner-local diagnosis/repair, establish a replacement revision, rerun every invalidated focused/matrix/full gate on that replacement, and stop for any required production secure-ID edit, wrapper/package/PDF change, new owner, architecture change, or missing authority.

Record the implementation base/frozen revision, four-path diff, unchanged-owner hashes, PDF object/size/offset facts, authority check, matrix hash/route table, exact command outcomes, and clean negative scope in `02-01-SUMMARY.md`. Return control to native execute-phase. Its ordinary gsd-verifier creates `02-VERIFICATION.md`; that result is the authorization endpoint. Do not create or request a final-review-specific reverify gate.
  </action>
  <acceptance_criteria>
    - PDF-01 proves both raw worktree blob IDs equal HEAD, both committed sizes/startxref targets remain valid, and both staged/unstaged path diffs are empty.
    - AUTH-01 and FLOW-01 show coherent active authority and the native research → requirements/roadmap → checked plan → bounded execution → ordinary verification chain without a parallel lifecycle artifact.
    - SCOPE-01 limits the committed implementation diff to the four approved owners and proves production ID code, wrapper, manifest/lock, attributes, and PDF bytes unchanged.
    - The complete ephemeral matrix result set is bound to the frozen `HEAD:.githooks/pre-commit` blob; any failed Task 3 assertion retains the exact external root for bounded rerun, while a fully successful Task 3 removes it and proves post-cleanup absence without leaving a repository harness artifact.
    - Parsed Native `smart-entry --json` identifies current Phase 2 lifecycle routing and exposes no v1.1 release, ship, or complete-milestone action.
    - The frozen revision passes `format:check`, lint, typecheck, the full unit suite, and build; focused/matrix evidence is either hash-valid for that revision or rerun after a relevant change.
    - `02-01-SUMMARY.md` contains objective revision/provenance evidence and ordinary Native verification is the next and final authorized stage.
    - No product expansion, dormant-seed activation, R01 work, dependency, runtime wrapper, lifecycle controller, PDF edit, special reverify gate, or release action occurs.
  </acceptance_criteria>
  <verify>
    <automated>
$ErrorActionPreference = "Stop"
$matrixRoot = 'C:\tmp\metronome-phase-02-hook-matrix'
$resolvedMatrixRoot = [IO.Path]::GetFullPath($matrixRoot).TrimEnd('\')
if ($resolvedMatrixRoot -ne 'C:\tmp\metronome-phase-02-hook-matrix') { throw "Unexpected matrix cleanup root" }
  $matrixResultsPath = Join-Path $resolvedMatrixRoot 'matrix-results.json'
  if (-not (Test-Path -LiteralPath $matrixResultsPath -PathType Leaf)) { throw "Task 2 matrix provenance is missing" }
  $matrixResults = @(Get-Content -Raw -LiteralPath $matrixResultsPath | ConvertFrom-Json)
  $requiredMatrixCases = @('exact-minimum','higher-same-major','higher-major','missing-node-fallback','npm-cmd-only-direct','pwsh-only-fallback','too-old-node','too-old-npm','malformed-candidate-version','prerelease-candidate-version','failed-candidate-probe','missing-direct-command','missing-engine-declaration','duplicate-engine-declaration','compound-engine-range','unsupported-engine-range','fallback-success','no-route','competing-npm-forms') | Sort-Object
  $observedMatrixCases = @($matrixResults | ForEach-Object { [string]$_.case }) | Sort-Object
  if ($matrixResults.Count -ne $requiredMatrixCases.Count -or @(Compare-Object -ReferenceObject $requiredMatrixCases -DifferenceObject $observedMatrixCases).Count -ne 0) { throw "Frozen matrix case set is incomplete, duplicated, or unexpected" }
  $headHookBlob = (git rev-parse 'HEAD:.githooks/pre-commit').Trim()
  if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($headHookBlob)) { throw "Unable to resolve frozen hook blob" }
  $worktreeHookBlob = (git hash-object --no-filters -- .githooks/pre-commit).Trim()
  if ($LASTEXITCODE -ne 0 -or $worktreeHookBlob -ne $headHookBlob) { throw "Worktree hook bytes do not equal the frozen HEAD blob" }
  if (@($matrixResults | Where-Object { [string]$_.hookHash -ne $headHookBlob }).Count -ne 0) { throw "Matrix provenance is not bound to HEAD:.githooks/pre-commit" }

  $authority = [ordered]@{
    PROJECT = Get-Content -Raw -LiteralPath .planning/PROJECT.md
    STATE = Get-Content -Raw -LiteralPath .planning/STATE.md
    ROADMAP = Get-Content -Raw -LiteralPath .planning/ROADMAP.md
    AGENTS = Get-Content -Raw -LiteralPath AGENTS.md
  }
  $authorityPatterns = [ordered]@{
    PROJECT = @('## Current Milestone: v1\.2 Release Assurance & Workflow Closure','(?is)v1\.1.{0,200}shipped','Native OpenGSD','(?is)(dormant.{0,160}R01|R01.{0,160}dormant)','Stop this authorization after native verification')
    STATE = @('\*\*Current focus:\*\* Phase 2','Repository Formatting Baseline \| 1/1','32 dormant product capability seeds','Fresh evidence-led R01','\[Workflow\]: Native OpenGSD verification is the authorization endpoint')
    ROADMAP = @('(?is)v1\.1 Repository Formatting Baseline.{0,200}shipped','Phase 2: Release Assurance & Workflow Closure','Native OpenGSD lifecycle through verification','dormant-seed expansion','authorization stops at that verification result')
    AGENTS = @('(?is)v1\.1.{0,200}shipped','(?is)v1\.2.{0,200}(active|current)','Native OpenGSD is the sole','(?is)(dormant.{0,160}R01|R01.{0,160}dormant)','(?is)authorization.{0,200}(stops|ends).{0,200}Native OpenGSD.{0,100}verification')
  }
  foreach ($authorityFile in $authorityPatterns.Keys) {
    foreach ($pattern in $authorityPatterns[$authorityFile]) {
      if ($authority[$authorityFile] -notmatch $pattern) { throw "$authorityFile active-authority assertion failed: $pattern" }
    }
  }
  if ($authority.AGENTS -match 'release exit is active|release exit is still pending') { throw "AGENTS retains stale v1.1 release routing" }

  $nodeExe = (Resolve-Path -LiteralPath '.tools/node-v24.17.0-win-x64/node.exe').Path
  $gsdTools = 'C:\Users\wsuto\.codex\gsd-core\bin\gsd-tools.cjs'
  if (-not (Test-Path -LiteralPath $gsdTools -PathType Leaf)) { throw "Native gsd-tools.cjs is unavailable" }
  $smartRaw = @(& $nodeExe $gsdTools query smart-entry --json)
  if ($LASTEXITCODE -ne 0 -or $smartRaw.Count -eq 0) { throw "Native smart-entry query failed" }
  $smart = (($smartRaw -join "`n") | ConvertFrom-Json)
  if ([int]$smart.signals.current_phase -ne 2 -or [string]$smart.signals.roadmap_next_incomplete_phase -ne '2') { throw "Native smart-entry is not bound to current Phase 2" }
  $routeParts = @([string]$smart.situation,[string]$smart.recommended,[string]$smart.summary)
  $routeParts += @($smart.actions | ForEach-Object { "$($_.id) $($_.label) $($_.command)" })
  $routeText = $routeParts -join ' '
  if ($routeText -notmatch '(?i)phase\s*2|phase-2') { throw "Native smart-entry does not expose the Phase 2 lifecycle" }
  if ($routeText -match '(?i)v1\.1|release[- ]exit|complete-milestone|(^|\W)(ship|shipping)(\W|$)') { throw "Native smart-entry routed to release work instead of Phase 2" }

  $phaseBase = (git log -1 --format=%H -- .planning/phases/02-release-assurance-workflow-closure/02-01-PLAN.md).Trim()
  $frozen = (git rev-parse HEAD).Trim()
  $expected = @('.githooks/pre-commit','.planning/PROJECT.md','AGENTS.md','tests/unit/quick-metronome-session.test.ts') | Sort-Object
  $actual = @(git diff --name-only "$phaseBase..$frozen" --) | Sort-Object
  if (@(Compare-Object -ReferenceObject $expected -DifferenceObject $actual).Count -ne 0) { throw "Frozen implementation diff is outside the four approved owners" }
  $preserved = @('package.json','package-lock.json','scripts/npm-local.ps1','src/lib/quick-metronome/session.ts','.gitattributes','test-fixtures/sheets/real-sheet.pdf','test-fixtures/sheets/two-page-sheet.pdf')
  git diff --exit-code "$phaseBase..$frozen" -- $preserved
  if ($LASTEXITCODE -ne 0) { throw "A preserved production/runtime/binary owner changed" }
  $pdfs = [ordered]@{
    'test-fixtures/sheets/real-sheet.pdf' = @{ Size = 598; StartXref = 415 }
    'test-fixtures/sheets/two-page-sheet.pdf' = @{ Size = 936; StartXref = 713 }
  }
  foreach ($path in $pdfs.Keys) {
    $headBlob = (git rev-parse "HEAD:$path").Trim()
    $worktreeBlob = (git hash-object --no-filters -- $path).Trim()
    if ($headBlob -ne $worktreeBlob) { throw "Raw PDF bytes differ from HEAD: $path" }
    $bytes = [IO.File]::ReadAllBytes((Resolve-Path -LiteralPath $path))
    $text = [Text.Encoding]::ASCII.GetString($bytes)
    $matches = [regex]::Matches($text, 'startxref\s+(\d+)\s+%%EOF\s*$')
    $offset = [int]$pdfs[$path].StartXref
    if ($bytes.Length -ne [int]$pdfs[$path].Size -or -not $text.StartsWith('%PDF-1.4') -or $matches.Count -ne 1 -or [int]$matches[0].Groups[1].Value -ne $offset -or [Text.Encoding]::ASCII.GetString($bytes,$offset,4) -ne 'xref') { throw "PDF size or offset invariant failed: $path" }
  }
  git diff --exit-code -- @($pdfs.Keys)
  if ($LASTEXITCODE -ne 0) { throw "PDF worktree diff exists" }
  git diff --cached --exit-code -- @($pdfs.Keys)
  if ($LASTEXITCODE -ne 0) { throw "PDF staged diff exists" }
  & .\scripts\npm-local.ps1 --% run format:check
  if ($LASTEXITCODE -ne 0) { throw "Frozen format check failed" }
  & .\scripts\npm-local.ps1 --% run lint
  if ($LASTEXITCODE -ne 0) { throw "Frozen lint failed" }
  & .\scripts\npm-local.ps1 --% run typecheck
  if ($LASTEXITCODE -ne 0) { throw "Frozen typecheck failed" }
  & .\scripts\npm-local.ps1 --% run test:unit
  if ($LASTEXITCODE -ne 0) { throw "Frozen full unit suite failed" }
  & .\scripts\npm-local.ps1 --% run build
  if ($LASTEXITCODE -ne 0) { throw "Frozen build failed" }
  $verified = (git rev-parse HEAD).Trim()
  if ($verified -ne $frozen) { throw "Candidate revision changed during gates" }
  $outsidePlanning = @(git status --porcelain=v1 --untracked-files=all -- . ':(exclude).planning/**')
  if ($outsidePlanning.Count -ne 0) { throw "Non-planning worktree is not clean after gates" }
if (Test-Path -LiteralPath $resolvedMatrixRoot) { Remove-Item -LiteralPath $resolvedMatrixRoot -Recurse -Force -ErrorAction Stop }
if (Test-Path -LiteralPath $resolvedMatrixRoot) { throw "Temporary route harness was not removed" }
    </automated>
  </verify>
  <done>The four-owner implementation revision has immutable binary/scope and proportional gate evidence, active authority and parsed smart-entry remain on Phase 2, matrix provenance is bound to the frozen HEAD hook before exact cleanup, and control is handed to the ordinary Native verifier without a release action or special reverify layer.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reliable secure quick-recording identity, engine-aware commit routing, byte-identical fixtures, and coherent Native lifecycle through verification | T1-T3 | COVERED | One ordered integrated assurance path ends at ordinary verification. |
| REQ | ID-01 | randomUUID precedence with existing prefix and deterministic proof | T1,T3 | COVERED | Existing production path is characterized and included in final unit evidence. |
| REQ | ID-02 | Exactly 16 secure bytes, 32 lowercase hex characters, linked artifact/recording IDs | T1,T3 | COVERED | Existing adjacent test and unchanged production bytes are required. |
| REQ | ID-03 | Existing secure-random error and no recording without a secure path | T1,T3 | COVERED | Existing failure test remains green; no weak fallback is added. |
| REQ | HOOK-01 | Staged simple minima and exact stable compatible Node/npm pair | T1,T2 | COVERED | Exact-minimum, higher-same-major, and higher-major cases prove numeric exact-candidate selection. |
| REQ | HOOK-02 | All unproven direct cases use existing fallback or fail non-zero | T1,T2 | COVERED | Independent too-old, malformed, prerelease, failed-probe, missing-command, missing/duplicate/compound/unsupported-engine, fallback-success, and no-route cases prove conservative routing. |
| REQ | HOOK-03 | Preserve staged snapshot, bootstrap, exclusion, cleanup, whitespace, and fast format scope | T1-T3 | COVERED | Structural preservation plus real hook integration and final scope proof. |
| REQ | PDF-01 | Raw blob equality, committed size/offset invariants, and empty path diffs | T3 | COVERED | Git object plus raw-byte checks; fixtures are never written. |
| REQ | AUTH-01 | Shipped/current/dormant/native/verification-stop authority coherence | T1,T3 | COVERED | Active AGENTS/PROJECT drift is corrected; native STATE/ROADMAP transitions are inspected. |
| REQ | FLOW-01 | Native research, requirements/roadmap, checked plan, execution, verification | T1-T3 | COVERED | Plan consumes existing native artifacts and hands execution evidence to the ordinary verifier. |
| REQ | SCOPE-01 | No product, seed, redesign, dependency, wrapper/controller, PDF, special reverify, or release expansion | T1-T3 | COVERED | Four-path committed diff and explicit negative-scope stop. |
| RESEARCH | REUSE | Use existing session/test/hook/wrapper/Git/native owners; reject all parallel owners | T1-T3 | COVERED | Exact compact reuse decision is copied into task boundaries. |
| RESEARCH | AUTHORITY | Reconcile stale active AGENTS clauses and PROJECT terminology before implementation | T1 | COVERED | First ordered substep; archives and native state ownership preserved. |
| RESEARCH | SECURE-ID | Add only one both-APIs precedence characterization | T1 | COVERED | Existing fallback/failure cases retained; production source immutable. |
| RESEARCH | SELECTOR | Staged manifest, exact paths, guarded probes, narrow stable parser, numeric comparison | T1 | COVERED | Three hook-local helpers and exact candidate state are specified. |
| RESEARCH | MATRIX | Exact minimum, higher same major, higher major, incompatible versions, malformed/prerelease/failed candidates, missing command, missing/duplicate/compound/unsupported staged declarations, fallback, failure, and competing routes | T1,T2,T3 | COVERED | Exact structured input tuples and route provenance are bound to the frozen hook blob before guarded cleanup. |
| RESEARCH | HOOK-BOUNDARY | Preserve snapshot/bootstrap/cleanup/PowerShell/format-only behavior | T1,T2 | COVERED | Selector-only edit and real integration gate. |
| RESEARCH | FIXTURES | Git raw identity plus valid 598/936 and 415/713 invariants | T3 | COVERED | No restore or binary write occurs. |
| RESEARCH | EVIDENCE | Focused and proportional full gates tied to candidate inputs/revision | T2,T3 | COVERED | Matrix hash plus one final non-browser gate sequence. |
| RESEARCH | NATIVE-STOP | Ordinary Native verification is the endpoint; release work is separate | T1,T3 | COVERED | No shipping task or special verification artifact. |
| CONTEXT | — | No CONTEXT.md exists by explicit owner choice | — | NOT PRESENT | PROJECT, REQUIREMENTS, ROADMAP, and completed research supply the durable decisions; no decision is manufactured. |
| PLANNING | probe fallback | `workflow.specless_probe_fallback=false` | T1-T3 | COVERED | No probe-derived predicate or extra validation layer is added. |
| EXCLUDED | deferred | Dormant product seeds, fresh R01, generalized SemVer ranges, and all release actions | — | EXCLUDED | Explicitly assigned to separate owner authorization and absent from tasks. |

<verification>
1. Native plan validation must pass `frontmatter.validate --schema plan` and `verify.plan-structure` before execution; security, Nyquist, product-domain, and specless-probe layers remain disabled by live milestone configuration.
2. Task 1 proves the ordered authority → secure-ID → exact-candidate tracer through the four durable owners and the real staged hook while preserved source/runtime/binary owners remain unchanged.
3. Task 2 proves the complete independently named route matrix against one observed hook hash, including exact compatible tuples, incompatible candidates, each staged-declaration failure, fallback/no-route provenance, and real wrapper/snapshot integration, then retains only the external evidence root for final binding.
4. Task 3 binds that matrix evidence to `HEAD:.githooks/pre-commit`, runs deterministic PROJECT/STATE/ROADMAP/AGENTS plus parsed Native smart-entry checks, removes the exact external harness root, and proves the four-path diff, PDF invariants, clean negative scope, and one frozen `format:check`/lint/typecheck/unit/build sequence.
5. Native execute-phase creates `02-01-SUMMARY.md`, then hands the committed revision to the ordinary gsd-verifier, which creates `02-VERIFICATION.md`; no plan task invokes or imitates verification and no final-review-specific reverify gate exists.
6. Stop after the Native verification result. Push, PR, final-head CI/review, merge, tag, archival, local-main synchronization, product work, dormant-seed activation, and R01 work remain unauthorized.
</verification>

<success_criteria>
- All 10 Phase 2 requirement IDs appear exactly once in frontmatter and have an objective task/verification path in the source audit.
- Active authority is coherent before code work and remains coherent at the frozen revision without rewriting valid archives or creating a lifecycle controller.
- The existing secure recording-ID implementation and fallback/error semantics are unchanged and fully characterized by the focused owner.
- The tracked hook selects only proven exact compatible candidates, conservatively reuses the unchanged PowerShell wrapper for all other supported cases, fails closed with no route, and preserves every established fast-gate invariant.
- Both PDF files are byte-identical to HEAD with valid committed sizes/offsets and empty staged/unstaged diffs.
- The implementation changes exactly four approved paths, adds no package/path owner, passes revision-bound proportional gates, reaches ordinary Native verification, and stops there.
</success_criteria>

<output>
Create `.planning/phases/02-release-assurance-workflow-closure/02-01-SUMMARY.md` when done. Native execute-phase then performs ordinary phase verification and stops at its result.
</output>
