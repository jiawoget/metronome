---
phase: 01-canonical-practice-presentation-formatting
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/domain/practice/validation.ts
  - src/services/practice-goals/service.ts
autonomous: true
requirements: [FMT-01, FMT-02, EVID-01, SLIM-01, QUAL-01, HEALTH-01, DELIV-01]
gap_closure: true
must_haves:
  truths:
    - "CAP-03 traceability is exact: the receipt's fourteen named `884805f..candidate` hunk records restore the blank line, nine finite-number guards, and four non-async CRUD declarations while admitted parser, ID, wiring, and tests remain unchanged."
    - "The corrected immutable revision preserves the exact legacy timestamp/duration and Goals behavior under focused checks and one unbypassed repository-wide hook run."
    - "CodeScene has 12 attributable baseline/final records for six existing sources and two attributable final records for the added Goals sources; every record carries field-complete revision, path, blob, capture, tool, score, findings, and raw provider payload data."
    - "The safeguarded candidate and corrected commit have equal parent, tree, path set, and Git blobs."
    - "The corrected revision remains exact 6M + 2A and strictly normalized net-negative, rolls back in a disposable context, and leaves relevant source/configuration clean."
    - "Refreshed HEALTH-01 and DELIV-01 conclusions pass only from complete corrected-revision evidence."
  prohibitions: []
  artifacts:
    - path: src/domain/practice/validation.ts
      provides: Corrected CAP-03 validation owner with rejected schema edits absent
      contains: parsePracticeGoalDraft
    - path: src/services/practice-goals/service.ts
      provides: Corrected CAP-03 ID owner with pre-phase CRUD declarations restored
      contains: createPracticeGoalId
    - path: .planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md
      provides: Fingerprinted gap-closure execution contract
    - path: .logs/gsd-observability/r01-gap-closure-20260722-01/evidence/01-02-final-evidence.json
      provides: Corrected immutable revision, gates, CodeScene, LOC, rollback, identity, and conclusions
    - path: .planning/phases/01-canonical-practice-presentation-formatting/01-02-SUMMARY.md
      provides: Native plan completion summary and evidence digest
  key_links:
    - from: src/components/home/practice-goal-editor.tsx
      to: src/domain/practice/validation.ts
      via: Existing parsePracticeGoalDraft call remains byte-identical
      pattern: parsePracticeGoalDraft
    - from: src/hooks/use-practice-session-dashboard.ts
      to: src/services/practice-goals/service.ts
      via: Existing createPracticeGoalId wiring remains byte-identical
      pattern: createPracticeGoalId
    - from: .logs/gsd-observability/r01-gap-closure-20260722-01/evidence/01-02-final-evidence.json
      to: corrected product commit
      via: Safeguard-to-commit parent/tree/path/blob equality and attributable provider records
      pattern: candidate_identity
---

<objective>
Close the two Phase 01 verification gaps without repeating completed formatter or Goals implementation.

Purpose: Compile the research-corrected CAP-03 boundary by removing rejected incidental product hunks, then collect complete final evidence once against the resulting immutable candidate.
Output: An admitted-only corrected product revision, exact focused/full-gate evidence, complete attributable CodeScene records, candidate-to-commit identity, final LOC/rollback proof, and refreshed HEALTH-01/DELIV-01 conclusions.
</objective>

<execution_context>
@C:/Users/wsuto/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wsuto/.codex/gsd-core/templates/summary.md
</execution_context>

<execution_contract evidence_receipt=".planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md" receipt_sha256="1bd6346b7b2df946c8ca55d05b7bcdc35502263c6c72aa531b7599fa62fdfa9c" no_auto_retry="true" />

<context>
@AGENTS.md
@skills/metronome-policy/SKILL.md
@.planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md
@.codescene/quality-gate-policy.md
@src/domain/practice/validation.ts
@src/services/practice-goals/service.ts
@src/components/home/practice-goal-editor.tsx
@src/hooks/use-practice-session-dashboard.ts
@tests/unit/home-dashboard.test.tsx
@tests/unit/session-comparison.test.ts
@tests/unit/architecture-boundaries.test.ts
@tests/unit/practice-goal-repository.test.ts
@tests/unit/practice-goal-service.test.ts
</context>

<assumption_delta_decision primary_noun="practice formatting behavior" decision="no-change" rationale="The lexical trigger is fallback wording, not a singular-to-plural identity transition." />

<flagged_assumptions source="spec-less" applicable="24" resolved="0" unresolved="24">
- FMT-01: boundary, empty, encoding, precision, idempotency, concurrency.
- FMT-02: boundary, adjacency, empty, ordering, precision.
- EVID-01: concurrency.
- SLIM-01: concurrency.
- QUAL-01: boundary, adjacency, empty, ordering, precision.
- HEALTH-01: boundary, adjacency, empty, ordering, precision.
- DELIV-01: unclassified/manual review.
</flagged_assumptions>

<prohibition_probe applicable="0" resolved="0" unresolved="0">Descriptor-less recall produced only routine engineering or canon security items; precision dropped them, so no bespoke flagged-unverified prohibition is invented.</prohibition_probe>

<tasks>

<task type="auto" id="01-02-T1" gate_stage="task" budget="standard" plane="product" resumable="true" resume_key="r01-cap03-incidental-hunk-correction" cache_key="cap03-corrected-candidate:20260722" inputs="receipt,current-product-fingerprint,reviewed-validation,reviewed-goal-service" outputs="corrected-validation,corrected-goal-service,candidate-fingerprint">
  <name>Task 01-02-T1: Remove only the rejected CAP-03 incidental hunks</name>
  <files>src/domain/practice/validation.ts, src/services/practice-goals/service.ts</files>
  <read_first>.planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md, src/domain/practice/validation.ts, src/services/practice-goals/service.ts, src/components/home/practice-goal-editor.tsx, src/hooks/use-practice-session-dashboard.ts, tests/unit/practice-goal-repository.test.ts, tests/unit/practice-goal-service.test.ts</read_first>
  <action>Require an empty index, the receipt SHA, and all four pre-correction fingerprints. Per the corrected CAP-03 USE_LOCAL admission, edit only the two named sources: restore the receipt's fourteen-row `884805f..candidate` hunk manifest (the import-separating blank line, nine pre-phase `.finite()` calls at its named schemas, and the four receipt-named non-async CRUD declarations). Derive the expected candidate by applying only those reversals to the reviewed blobs, then require byte-for-byte equality of each working source to that expected candidate; this proves both the named anchors and absence of any other hunk in either owner. Preserve `parsePracticeGoalDraft`, its admitted schemas/types/helper, `createPracticeGoalId`, WebCrypto-only failure behavior, hook/editor wiring, both Goals components, and every test byte-for-byte. Record the two canonical corrected blobs, candidate fingerprint, and receipt-manifest hash for T2. Any third product path or different hunk terminates the task.</action>
  <verify><automated>
$reviewed = '884805f16d4327e0fa57046f937e38e4f1106540'
git diff --cached --quiet
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
function Get-GitText([string] $revision, [string] $path) {
  $lines = @(git show ($revision + ':' + $path))
  if ($LASTEXITCODE -ne 0) { throw "Cannot read $revision:$path" }
  return ([string]::Join([string][char]10, $lines) + [string][char]10)
}
function Restore-ExactHunk([string] $source, [string] $reviewedFragment, [string] $restoredFragment, [string] $id) {
  if ([regex]::Matches($source, [regex]::Escape($reviewedFragment)).Count -ne 1) {
    throw "Receipt hunk $id is not uniquely anchored in the reviewed blob."
  }
  return $source.Replace($reviewedFragment, $restoredFragment)
}
$reviewedValidation = Get-GitText $reviewed 'src/domain/practice/validation.ts'
$expectedValidation = $reviewedValidation
$validationFragments = @(
  @{ id = 'CAP-03-V00'; reviewed = ('import { z } from "zod";' + [char]10 + 'import {'); restored = ('import { z } from "zod";' + [char]10 + [char]10 + 'import {') },
  @{ id = 'CAP-03-V01'; reviewed = 'const targetBpmSchema = z.number().int().min(30).max(300).nullable();'; restored = 'const targetBpmSchema = z.number().finite().int().min(30).max(300).nullable();' },
  @{ id = 'CAP-03-V02-V03'; reviewed = ('    startMs: z.number().int().nonnegative(),' + [char]10 + '    endMs: z.number().int().nonnegative()'); restored = ('    startMs: z.number().finite().int().nonnegative(),' + [char]10 + '    endMs: z.number().finite().int().nonnegative()') },
  @{ id = 'CAP-03-V04-V06'; reviewed = ('    durationMs: z.number().int().nonnegative(),' + [char]10 + '    bpm: z.number().int().min(30).max(300).nullable(),' + [char]10 + '    timeSignature: practiceTimeSignatureSchema.nullable(),' + [char]10 + '    recordingCount: z.number().int().nonnegative(),'); restored = ('    durationMs: z.number().finite().int().nonnegative(),' + [char]10 + '    bpm: z.number().finite().int().min(30).max(300).nullable(),' + [char]10 + '    timeSignature: practiceTimeSignatureSchema.nullable(),' + [char]10 + '    recordingCount: z.number().finite().int().nonnegative(),') },
  @{ id = 'CAP-03-V07-V08'; reviewed = ('  durationMs: z.number().int().nonnegative(),' + [char]10 + '  bpm: z.number().int().min(30).max(300).nullable(),'); restored = ('  durationMs: z.number().finite().int().nonnegative(),' + [char]10 + '  bpm: z.number().finite().int().min(30).max(300).nullable(),') },
  @{ id = 'CAP-03-V09'; reviewed = '  target: z.number().int().positive(),'; restored = '  target: z.number().finite().int().positive(),' }
)
foreach ($fragment in $validationFragments) {
  $expectedValidation = Restore-ExactHunk $expectedValidation $fragment.reviewed $fragment.restored $fragment.id
}
$reviewedService = Get-GitText $reviewed 'src/services/practice-goals/service.ts'
$expectedService = $reviewedService
foreach ($method in @('listPracticeGoals', 'getPracticeGoal', 'savePracticeGoal', 'deletePracticeGoal')) {
  $expectedService = Restore-ExactHunk $expectedService ("    async $method(") ("    $method(") "CAP-03-S-$method"
}
$validation = (Get-Content -Raw -LiteralPath "src/domain/practice/validation.ts") -replace ([char]13 + [char]10), [string][char]10
$service = (Get-Content -Raw -LiteralPath "src/services/practice-goals/service.ts") -replace ([char]13 + [char]10), [string][char]10
if ($validation -cne $expectedValidation -or $service -cne $expectedService) { exit 1 }
if ([regex]::Matches($validation, '\.finite\(').Count -ne 9) { exit 1 }
if ([regex]::Matches($service, 'async\s+(?:listPracticeGoals|getPracticeGoal|savePracticeGoal|deletePracticeGoal)\s*\(').Count -ne 0) { exit 1 }
if ([regex]::Matches($validation, 'export function parsePracticeGoalDraft').Count -ne 1) { exit 1 }
if ([regex]::Matches($service, 'export function createPracticeGoalId').Count -ne 1) { exit 1 }
$changed = @(git diff --name-only -- src | Sort-Object)
$expected = @('src/domain/practice/validation.ts', 'src/services/practice-goals/service.ts')
if (($changed -join "`n") -ne ($expected -join "`n")) { exit 1 }
git diff --check -- @expected
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - The source correction is exactly two modified paths and byte-for-byte equals the reviewed blobs with only the receipt's fourteen named CAP-03 reversals applied.
    - The admitted parser, ID provider, exact wiring, components, and tests are unchanged; no other product or formatting hunk exists.
    - The corrected candidate fingerprint and both working blob IDs are retained for the immutable transaction.
  </acceptance_criteria>
  <done>CAP-03 product hunks now match the corrected research admission and are ready for one final evidence transaction.</done>
</task>

<task type="auto" id="01-02-T2" gate_stage="pre_merge" budget="external" plane="external" resumable="true" resume_key="r01-corrected-final-evidence" cache_key="corrected-final-evidence:20260722" inputs="t1-candidate,3370-baseline,receipt,codescene-policy,five-focused-tests" outputs="corrected-commit,final-evidence,loc-rollback,health-delivery-conclusions" external_job_id="codescene-r01-gap-closure-20260722-01">
  <name>Task 01-02-T2: Prove the corrected immutable revision once</name>
  <files>.logs/gsd-observability/r01-gap-closure-20260722-01/evidence/01-02-final-evidence.json</files>
  <read_first>AGENTS.md, skills/metronome-policy/SKILL.md, .planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md, .codescene/quality-gate-policy.md, src/components/home/home-dashboard.tsx, src/components/home/practice-goal-editor.tsx, src/components/home/practice-goals-panel.tsx, src/domain/practice/format.ts, src/domain/practice/session-comparison.ts, src/domain/practice/validation.ts, src/hooks/use-practice-session-dashboard.ts, src/services/practice-goals/service.ts, tests/unit/home-dashboard.test.tsx, tests/unit/session-comparison.test.ts, tests/unit/architecture-boundaries.test.ts, tests/unit/practice-goal-repository.test.ts, tests/unit/practice-goal-service.test.ts</read_first>
  <action>Require completed T1, its two canonical source blobs, a named branch, and clean relevant inputs. Start the single controller-owned external job at pre-merge. Stage only the two corrections; record the raw staged `name-status`, parent SHA/tree, staged tree, and staged blobs, then run the official CodeScene safeguard once against that identity. Run the receipt's focused five-file command once. Commit once through the unbypassed repository hook and embed its complete output as the sole repository-wide gate run. Resolve the corrected commit, recompute `parent..final` name-status, paths, tree, blobs, baseline ancestry, scoped cleanliness, and every 14-hunk fragment hash from Git content; compare each fact with evidence instead of an attestation boolean. Against `3370d2f93fd6740d96150d9ee69e31238b258c6a`, capture the complete normalized 6M + 2A path/status/LOC/hunk inventory and derive its strict net-negative value from Git. Use a disposable `git worktree` rollback with recorded add/test/cleanup commands, outputs, checked-out SHA/tree, and exit codes. Run one direct CodeScene change set plus the 14 receipt-specified official per-file captures. Derive HEALTH-01 and DELIV-01 only after this receipt-bound schema validates. Failure or interruption records a terminal external outcome; another attempt requires a new controller decision and identity.</action>
  <verify><automated>
$evidencePath = '.logs/gsd-observability/r01-gap-closure-20260722-01/evidence/01-02-final-evidence.json'
$evidence = Get-Content -Raw -LiteralPath $evidencePath | ConvertFrom-Json
function Require-Value($value, [string] $name) { if ($null -eq $value -or ([string]$value).Length -eq 0) { throw "Missing $name" } }
function Require-Equal($actual, $expected, [string] $name) { if ($actual -cne $expected) { throw "Unexpected $name" } }
function Require-Property($object, [string] $name) { if ($null -eq $object.PSObject.Properties[$name]) { throw "Missing property $name" } }
function Get-GitOutput([string[]] $gitArgs) {
  $result = @(& git @gitArgs)
  if ($LASTEXITCODE -ne 0) { throw "Git failed: $($gitArgs -join ' ')" }
  return ($result -join [char]10).TrimEnd([char]13, [char]10)
}
function Get-GitText([string] $revision, [string] $path) { return (Get-GitOutput @('show', ($revision + ':' + $path))) + [char]10 }
function Get-Sha256([string] $text) {
  $hash = [System.Security.Cryptography.SHA256]::Create()
  try { return ($hash.ComputeHash([System.Text.UTF8Encoding]::new($false).GetBytes($text)) | ForEach-Object { $_.ToString('x2') }) -join '' } finally { $hash.Dispose() }
}
function Assert-IdentityRows($rows, [string] $name, $expectedRows) {
  $actual = @($rows | Sort-Object path); $expected = @($expectedRows | Sort-Object path)
  if ($actual.Count -ne $expected.Count) { throw "Unexpected $name row count" }
  for ($index = 0; $index -lt $expected.Count; $index++) {
    Require-Equal $actual[$index].path $expected[$index].path "$name path"
    Require-Equal $actual[$index].status $expected[$index].status "$name status"
    Require-Equal $actual[$index].blob $expected[$index].blob "$name blob"
  }
}
function Assert-Findings($items, [string] $name) {
  if ($null -eq $items) { throw "Missing $name findings" }
  foreach ($finding in @($items)) { Require-Value $finding.id "$name finding ID"; Require-Value $finding.severity "$name finding severity" }
}
$receiptHash = (Get-FileHash -Algorithm SHA256 -LiteralPath '.planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md').Hash.ToLowerInvariant()
$reviewed = '884805f16d4327e0fa57046f937e38e4f1106540'; $baseline = '3370d2f93fd6740d96150d9ee69e31238b258c6a'; $final = Get-GitOutput @('rev-parse', 'HEAD')
$expectedPaths = @('src/domain/practice/validation.ts', 'src/services/practice-goals/service.ts')
$canonicalBlobs = @{'src/domain/practice/validation.ts' = '2872352f3764edf20cea44c1d928c524287d773f'; 'src/services/practice-goals/service.ts' = 'd676e0b272b7de7030e835b3ea9dba0be743ae79'}
$finalText = @{}
foreach ($path in $expectedPaths) { Require-Equal (Get-GitOutput @('rev-parse', ($final + ':' + $path))) $canonicalBlobs[$path] "canonical $path blob"; $finalText[$path] = Get-GitText $final $path }
$candidateFingerprint = Get-Sha256 (($expectedPaths | ForEach-Object { $_ + '=' + $canonicalBlobs[$_] }) -join [char]10)
Require-Equal $evidence.cap_hunk_mapping.reviewed_revision $reviewed 'CAP reviewed revision'
Require-Equal $evidence.cap_hunk_mapping.receipt_sha256 $receiptHash 'CAP receipt hash'
Require-Equal $evidence.cap_hunk_mapping.canonical_blobs.validation $canonicalBlobs['src/domain/practice/validation.ts'] 'CAP validation blob'
Require-Equal $evidence.cap_hunk_mapping.canonical_blobs.service $canonicalBlobs['src/services/practice-goals/service.ts'] 'CAP service blob'
Require-Equal $evidence.cap_hunk_mapping.candidate_fingerprint $candidateFingerprint 'CAP candidate fingerprint'
$expectedHunks = @(
  @{ id = 'CAP-03-V00'; path = 'src/domain/practice/validation.ts'; anchor = 'import { z } from "zod";'; fragment = ('import { z } from "zod";' + [char]10 + [char]10 + 'import {') },
  @{ id = 'CAP-03-V01'; path = 'src/domain/practice/validation.ts'; anchor = 'targetBpmSchema'; fragment = 'const targetBpmSchema = z.number().finite().int().min(30).max(300).nullable();' },
  @{ id = 'CAP-03-V02'; path = 'src/domain/practice/validation.ts'; anchor = 'measureRangeMsSchema.startMs'; fragment = '    startMs: z.number().finite().int().nonnegative(),' },
  @{ id = 'CAP-03-V03'; path = 'src/domain/practice/validation.ts'; anchor = 'measureRangeMsSchema.endMs'; fragment = '    endMs: z.number().finite().int().nonnegative()' },
  @{ id = 'CAP-03-V04'; path = 'src/domain/practice/validation.ts'; anchor = 'practiceSessionSchema.durationMs'; fragment = '    durationMs: z.number().finite().int().nonnegative(),' },
  @{ id = 'CAP-03-V05'; path = 'src/domain/practice/validation.ts'; anchor = 'practiceSessionSchema.bpm'; fragment = '    bpm: z.number().finite().int().min(30).max(300).nullable(),' },
  @{ id = 'CAP-03-V06'; path = 'src/domain/practice/validation.ts'; anchor = 'practiceSessionSchema.recordingCount'; fragment = '    recordingCount: z.number().finite().int().nonnegative(),' },
  @{ id = 'CAP-03-V07'; path = 'src/domain/practice/validation.ts'; anchor = 'sheetRecordingMetadataSchema.durationMs'; fragment = '  durationMs: z.number().finite().int().nonnegative(),' },
  @{ id = 'CAP-03-V08'; path = 'src/domain/practice/validation.ts'; anchor = 'sheetRecordingMetadataSchema.bpm'; fragment = '  bpm: z.number().finite().int().min(30).max(300).nullable(),' },
  @{ id = 'CAP-03-V09'; path = 'src/domain/practice/validation.ts'; anchor = 'localPracticeGoalSchema.target'; fragment = '  target: z.number().finite().int().positive(),' },
  @{ id = 'CAP-03-S01'; path = 'src/services/practice-goals/service.ts'; anchor = 'listPracticeGoals'; fragment = '    listPracticeGoals() {' },
  @{ id = 'CAP-03-S02'; path = 'src/services/practice-goals/service.ts'; anchor = 'getPracticeGoal'; fragment = '    getPracticeGoal(goalId) {' },
  @{ id = 'CAP-03-S03'; path = 'src/services/practice-goals/service.ts'; anchor = 'savePracticeGoal'; fragment = '    savePracticeGoal(goal) {' },
  @{ id = 'CAP-03-S04'; path = 'src/services/practice-goals/service.ts'; anchor = 'deletePracticeGoal'; fragment = '    deletePracticeGoal(goalId) {' }
)
$hunkRecords = @($evidence.cap_hunk_mapping.records)
if ($hunkRecords.Count -ne $expectedHunks.Count) { throw 'Incorrect CAP hunk record count' }
foreach ($expected in $expectedHunks) {
  $record = @($hunkRecords | Where-Object { $_.id -ceq $expected.id }); if ($record.Count -ne 1) { throw "Missing CAP hunk $($expected.id)" }
  $matches = [regex]::Matches($finalText[$expected.path], [regex]::Escape($expected.fragment)); if ($matches.Count -ne 1) { throw "CAP fragment $($expected.id) is not unique in final blob" }
  Require-Equal $record[0].path $expected.path "$($expected.id) path"; Require-Equal $record[0].context_anchor $expected.anchor "$($expected.id) anchor"
  Require-Equal $record[0].candidate_revision $final "$($expected.id) revision"; Require-Equal $record[0].candidate_blob $canonicalBlobs[$expected.path] "$($expected.id) blob"
  Require-Equal $record[0].candidate_fragment_sha256 (Get-Sha256 $matches[0].Value) "$($expected.id) fragment hash"
}
& git merge-base --is-ancestor $baseline $final; if ($LASTEXITCODE -ne 0) { throw 'Baseline is not an ancestor of final' }
$branch = Get-GitOutput @('branch', '--show-current'); if ([string]::IsNullOrWhiteSpace($branch)) { throw 'Final revision is detached' }; Require-Equal $evidence.branch $branch 'named branch'
$parent = Get-GitOutput @('rev-parse', ($final + '^')); $tree = Get-GitOutput @('rev-parse', ($final + '^{tree}'))
$finalNameStatus = Get-GitOutput @('diff-tree', '--no-commit-id', '--name-status', '-r', '--no-renames', $parent, $final)
$expectedNameStatus = "M`t$($expectedPaths[0])`nM`t$($expectedPaths[1])"; Require-Equal $finalNameStatus $expectedNameStatus 'parent..final name-status'
$finalRows = @($expectedPaths | ForEach-Object { [pscustomobject]@{ path = $_; status = 'M'; blob = $canonicalBlobs[$_] } })
Require-Equal $evidence.candidate_identity.parent_sha $parent 'candidate parent'; Require-Equal $evidence.candidate_identity.staged_tree $tree 'candidate staged tree'; Require-Equal $evidence.candidate_identity.staged_name_status $finalNameStatus 'candidate staged name-status'
Require-Equal $evidence.commit_identity.sha $final 'commit SHA'; Require-Equal $evidence.commit_identity.parent_sha $parent 'commit parent'; Require-Equal $evidence.commit_identity.tree $tree 'commit tree'; Require-Equal $evidence.commit_identity.diff_name_status $finalNameStatus 'commit diff name-status'
Require-Equal $evidence.safeguard.parent_sha $parent 'safeguard parent'; Require-Equal $evidence.safeguard.staged_tree $tree 'safeguard tree'; Require-Equal $evidence.safeguard.name_status $finalNameStatus 'safeguard name-status'
Require-Value $evidence.safeguard.provider 'safeguard provider'; Require-Value $evidence.safeguard.tool 'safeguard tool'; Require-Value $evidence.safeguard.captured_at 'safeguard capture time'; Require-Value $evidence.safeguard.raw_payload 'safeguard raw payload'
Assert-IdentityRows $evidence.candidate_identity.paths 'candidate identity' $finalRows; Assert-IdentityRows $evidence.commit_identity.paths 'commit identity' $finalRows; Assert-IdentityRows $evidence.safeguard.paths 'safeguard identity' $finalRows
$scopedStatus = Get-GitOutput (@('status', '--porcelain=v1', '--untracked-files=all', '--', 'src', '.codescene', 'package.json', 'package-lock.json', 'tsconfig.json', 'vite.config.ts'))
Require-Equal $scopedStatus '' 'scoped clean status'; Require-Equal $evidence.scoped_cleanliness.raw_status $scopedStatus 'recorded scoped clean status'
$focused = $evidence.gates.focused; $hook = $evidence.gates.hook
foreach ($gate in @($focused, $hook)) { Require-Value $gate.command 'gate command'; Require-Value $gate.raw_output 'gate raw output'; Require-Equal $gate.exit_code 0 'gate exit code'; Require-Equal $gate.attempt 1 'gate attempt' }
$focusedCommand = [regex]::Replace($focused.command, '\s+', ' ').Trim()
if ($focusedCommand -notmatch 'scripts[\\/]+npm-local\.ps1\s+--%\s+run\s+test:unit\s+--') { throw 'Focused command is not the documented project entrypoint' }
foreach ($path in @('home-dashboard.test.tsx', 'session-comparison.test.ts', 'architecture-boundaries.test.ts', 'practice-goal-repository.test.ts', 'practice-goal-service.test.ts')) { if ($focusedCommand -notmatch [regex]::Escape($path)) { throw "Focused command omits $path" } }
if ($focused.raw_output -notmatch '(?m)Test Files\s+5\s+passed' -or $focused.raw_output -notmatch '(?m)Tests\s+\d+\s+passed') { throw 'Focused output lacks success markers' }
if ($hook.command -notmatch '(^|\s)git\s+commit(?:\s|$)') { throw 'Hook command is not a commit' }
foreach ($marker in @('validate:debt-gates', 'lint:debt:changed', 'lint:xo:changed', 'lint', 'typecheck', 'test:unit', 'build')) { if ($hook.raw_output -notmatch [regex]::Escape($marker)) { throw "Hook output lacks $marker" } }
Require-Equal $hook.bypassed $false 'hook bypass'
$existingPaths = @('src/components/home/home-dashboard.tsx', 'src/domain/practice/format.ts', 'src/domain/practice/session-comparison.ts', 'src/domain/practice/validation.ts', 'src/hooks/use-practice-session-dashboard.ts', 'src/services/practice-goals/service.ts')
$addedPaths = @('src/components/home/practice-goal-editor.tsx', 'src/components/home/practice-goals-panel.tsx')
Require-Value $evidence.codescene.provider 'CodeScene provider'; Require-Value $evidence.codescene.tool 'CodeScene tool'; Require-Value $evidence.codescene.direct_changeset.raw_payload 'CodeScene direct payload'; Require-Value $evidence.codescene.direct_changeset.quality_gates 'CodeScene quality gates'; Require-Value $evidence.codescene.direct_changeset.results 'CodeScene results'
$records = @($evidence.codescene.records); if ($records.Count -ne 14) { throw 'Incorrect CodeScene record count' }
foreach ($path in $existingPaths) {
  $pair = @($records | Where-Object { $_.path -ceq $path }); $baselineRecord = @($pair | Where-Object { $_.attribution -ceq 'baseline' }); $finalRecord = @($pair | Where-Object { $_.attribution -ceq 'final' })
  if ($pair.Count -ne 2 -or $baselineRecord.Count -ne 1 -or $finalRecord.Count -ne 1) { throw "Missing CodeScene pair for $path" }
  foreach ($record in @($baselineRecord[0], $finalRecord[0])) {
    $revision = if ($record.attribution -ceq 'baseline') { $baseline } else { $final }
    Require-Equal $record.revision $revision "CodeScene $path revision"; Require-Equal $record.blob (Get-GitOutput @('rev-parse', ($revision + ':' + $path))) "CodeScene $path blob"; Require-Value $record.captured_at "CodeScene $path capture"; Require-Equal $record.provider $evidence.codescene.provider "CodeScene $path provider"; Require-Equal $record.tool $evidence.codescene.tool "CodeScene $path tool"; Require-Value $record.score.value "CodeScene $path score"; Require-Value $record.raw_payload "CodeScene $path payload"; Assert-Findings $record.findings.items "CodeScene $path"
  }
  if ([decimal]$finalRecord[0].score.value -lt [decimal]$baselineRecord[0].score.value) { throw "CodeScene score declined for $path" }
  $baselineSevere = @($baselineRecord[0].findings.items | Where-Object { $_.severity -in @('critical', 'high') } | ForEach-Object { $_.id }); $newSevere = @($finalRecord[0].findings.items | Where-Object { $_.severity -in @('critical', 'high') -and $_.id -notin $baselineSevere })
  if ($newSevere.Count -ne 0) { throw "New severe CodeScene finding for $path" }
}
foreach ($path in $addedPaths) {
  $record = @($records | Where-Object { $_.path -ceq $path -and $_.attribution -ceq 'final' }); if ($record.Count -ne 1) { throw "Missing CodeScene final record for $path" }
  Require-Equal $record[0].revision $final "CodeScene added $path revision"; Require-Equal $record[0].blob (Get-GitOutput @('rev-parse', ($final + ':' + $path))) "CodeScene added $path blob"; Require-Value $record[0].captured_at "CodeScene added $path capture"; Require-Equal $record[0].provider $evidence.codescene.provider "CodeScene added $path provider"; Require-Equal $record[0].tool $evidence.codescene.tool "CodeScene added $path tool"; Require-Value $record[0].score.value "CodeScene added $path score"; Require-Value $record[0].raw_payload "CodeScene added $path payload"; Assert-Findings $record[0].findings.items "CodeScene added $path"
  if (@($record[0].findings.items | Where-Object { $_.severity -in @('critical', 'high') }).Count -ne 0) { throw "Severe CodeScene finding for added $path" }
}
$homeFinal = @($records | Where-Object { $_.path -ceq 'src/components/home/home-dashboard.tsx' -and $_.attribution -ceq 'final' }); if ($homeFinal.Count -ne 1 -or [decimal]$homeFinal[0].score.value -lt 7.0) { throw 'Home CodeScene score is below 7.0' }
$locPaths = @($existingPaths + $addedPaths); $changeStatus = @{}; foreach ($line in ((Get-GitOutput @('diff', '--name-status', '--no-renames', $baseline, $final, '--', 'src')) -split [char]10)) { if ($line) { $parts = $line -split "`t", 2; if ($parts.Count -ne 2) { throw 'Malformed name-status row' }; $changeStatus[$parts[1]] = $parts[0] } }
if ($changeStatus.Count -ne 8) { throw 'Incorrect 6M + 2A path count' }
$numstat = @{}; foreach ($line in ((Get-GitOutput @('diff', '--numstat', '--no-renames', $baseline, $final, '--', 'src')) -split [char]10)) { if ($line) { $parts = $line -split "`t", 3; if ($parts.Count -ne 3) { throw 'Malformed numstat row' }; $numstat[$parts[2]] = $parts } }
$locRecords = @($evidence.loc.inventory); if ($locRecords.Count -ne 8 -or $numstat.Count -ne 8) { throw 'Incomplete LOC inventory' }; $computedNet = 0
foreach ($path in $locPaths) {
  $expectedStatus = if ($path -in $existingPaths) { 'M' } else { 'A' }; Require-Equal $changeStatus[$path] $expectedStatus "LOC $path status"
  $record = @($locRecords | Where-Object { $_.path -ceq $path }); if ($record.Count -ne 1) { throw "Missing LOC record for $path" }
  Require-Equal $record[0].status $expectedStatus "LOC record $path status"; Require-Equal ([int]$record[0].added) ([int]$numstat[$path][0]) "LOC $path added"; Require-Equal ([int]$record[0].removed) ([int]$numstat[$path][1]) "LOC $path removed"
  $hunkCount = @((Get-GitOutput @('diff', '--unified=0', '--no-renames', $baseline, $final, '--', $path) -split [char]10) | Where-Object { $_ -match '^@@ ' }).Count; Require-Equal ([int]$record[0].hunks) $hunkCount "LOC $path hunks"; $computedNet += [int]$numstat[$path][0] - [int]$numstat[$path][1]
}
Require-Equal ([int]$evidence.loc.normalized_net) $computedNet 'normalized net'; if ($computedNet -ge 0) { throw 'Normalized net is not negative' }
$rollback = $evidence.rollback; foreach ($property in @('worktree_add_command', 'worktree_add_output', 'checked_out_sha', 'tree', 'test_command', 'test_output', 'test_exit_code', 'cleanup_command', 'cleanup_output', 'cleanup_exit_code')) { Require-Property $rollback $property }
Require-Equal $rollback.checked_out_sha $parent 'rollback checked-out SHA'; Require-Equal $rollback.tree (Get-GitOutput @('rev-parse', ($parent + '^{tree}'))) 'rollback tree'; if ($rollback.worktree_add_command -notmatch 'git\s+worktree\s+add' -or $rollback.worktree_add_command -notmatch '--detach' -or $rollback.worktree_add_command -notmatch [regex]::Escape($parent)) { throw 'Rollback add command is incomplete' }
if ($rollback.test_command -notmatch 'scripts[\\/]+npm-local\.ps1' -or $rollback.test_output -notmatch '(?m)Test Files\s+5\s+passed' -or $rollback.test_exit_code -ne 0) { throw 'Rollback focused test evidence is incomplete' }
if ($rollback.cleanup_command -notmatch 'git\s+worktree\s+remove' -or $rollback.cleanup_exit_code -ne 0) { throw 'Rollback cleanup evidence is incomplete' }
if ($evidence.requirements.'HEALTH-01' -ne 'passed' -or $evidence.requirements.'DELIV-01' -ne 'passed' -or $evidence.status -ne 'passed') { throw 'Incomplete final requirement conclusions' }
git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  </automated></verify>
  <acceptance_criteria>
    - Focused checks run once and the unbypassed hook runs once; both pass against the exact safeguarded candidate.
    - Candidate and commit parent/tree/path/blob identities are equal and durably recorded; the corrected revision remains the named branch tip with clean relevant state.
    - Six existing sources have complete baseline+final records and both added sources have complete final records; all records are officially attributable, no changed source declines or gains a severe finding, and Home is at least 7.0.
    - Exact 6M + 2A normalized production LOC remains strictly negative, disposable rollback passes, and refreshed HEALTH-01/DELIV-01 conclusions are evidence-complete.
  </acceptance_criteria>
  <done>The two verification gaps are closed or fail closed at the single corrected-revision evidence boundary.</done>
</task>

</tasks>

<threat_model asvs="L1" block_on="high">
| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-01-02-01 | Tampering | CAP-03 product correction | high | mitigate | Exact two-path/hunk allowlist, pinned fingerprints, preserved admitted symbols/tests. |
| T-01-02-02 | Repudiation | Candidate and provider evidence | high | mitigate | Parent/tree/path/blob equality plus exact revision/path/blob/time/tool and complete payloads. |
| T-01-02-03 | Tampering | External CodeScene boundary | medium | mitigate | Controller-owned one-attempt job; missing, stale, or unattributable output blocks. |
| T-01-02-04 | Denial of service | Provider/gate interruption | low | accept | Terminal blocked/interrupted evidence is tolerable; it cannot produce a passing conclusion. |
</threat_model>

## Artifacts this phase produces

- `.planning/phases/01-canonical-practice-presentation-formatting/01-02-EXECUTION-RECEIPT.md`
- `.logs/gsd-observability/r01-gap-closure-20260722-01/evidence/01-02-final-evidence.json`
- `.planning/phases/01-canonical-practice-presentation-formatting/01-02-SUMMARY.md`
- Product files/symbols added: **None**.

## Multi-Source Coverage Audit

| Source | ID | Contract | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Preserve exact presentation, canonical callers, and net reduction | 01-02 | COVERED |
| REQ | FMT-01 | Exact legacy behavior on corrected revision | T1-T2 | COVERED |
| REQ | FMT-02 | One canonical owner; no parallel path | T1-T2 | COVERED |
| REQ | EVID-01 | Fingerprinted immutable evidence | T1-T2 | COVERED |
| REQ | SLIM-01 | Exact 6M + 2A normalized net-negative proof | T2 | COVERED |
| REQ | QUAL-01 | Focused checks and repository hook once | T2 | COVERED |
| REQ | HEALTH-01 | Complete attributable CodeScene evidence | T2 | COVERED |
| REQ | DELIV-01 | Identity, rollback, cleanliness, refreshed conclusion | T2 | COVERED |
| RESEARCH | CAP-03 correction | Admit parser/ID; reject finite/async incidental hunks | T1 | COVERED |
| RESEARCH | Evidence constraints | Exact path/blob/provider and one final transaction | T2 | COVERED |
| CONTEXT | Controller gap decision | Add one gap plan; no repeated implementation/discovery | T1-T2 | COVERED |

<verification>
- [ ] Repository liveness validation passes before dispatch; a nonzero result is PLAN_BLOCKED.
- [ ] T1 produces only the two admitted corrections and preserves every admitted symbol, wiring path, and test.
- [ ] T2 creates one corrected immutable revision, one focused run, one hook run, one external job, and one evidence artifact.
- [ ] All 24 spec-less assumptions remain explicitly unresolved; prohibition precision remains zero.
- [ ] Evidence proves CAP traceability, 14 per-file records, candidate-to-commit equality, negative LOC, rollback, HEALTH-01, and DELIV-01.
</verification>

<success_criteria>
- The corrected product diff contains no rejected incidental hunk and no unadmitted product change.
- All seven requirements remain covered; the two verifier gaps close only from complete evidence tied to the corrected immutable revision.
</success_criteria>

<output>
Create `.planning/phases/01-canonical-practice-presentation-formatting/01-02-SUMMARY.md` and record the final evidence artifact SHA-256 when done.
</output>
