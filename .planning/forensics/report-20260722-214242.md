# Forensic Report — workflow debt remained after repository cleanup

**Generated:** 2026-07-22T21:42:42+08:00
**Problem:** A fresh independent audit found that the workflow repair had been declared complete while release enforcement, OpenGSD runtime binding, mutating-agent boundaries, observability attribution, Lumen storage, and legacy external state still carried active debt.
**Owner decision:** On 2026-07-22 the owner approved the native-first repair and the exact cleanup boundary below. R01 remains paused and is only the incident specimen.

## Evidence summary

- Repository baseline at authorization: clean `main`, `main == origin/main == a766e9e40e1700883a11d53226415687fa2321f5`, one registered primary worktree.
- PR #131 merged with exact-head CI but zero submitted reviews. The required Codex terminal review was not satisfied at merge.
- PR #132 merged with an unresolved, non-outdated Codex P2 thread. A later owner-authored `FINDING_FREE` review was not a Codex result.
- The active `Protect Main` ruleset required zero approvals, did not require thread resolution or named status checks, and allowed a permanent maintainer bypass.
- All 71 installed Codex OpenGSD adapters used the obsolete `fork_context`/omit-model contract; all 34 installed agent TOMLs omitted model and effort. The live spawn schema supports `model`, `reasoning_effort`, and `fork_turns`.
- `gsd-code-fixer` was read-only while instructed to edit and commit, and its prompt unconditionally required a worktree despite `workflow.use_worktrees=false`. Fixer and debugger did not receive the project policy skill.
- Active Lumen storage pointed into an experimental provider cache. That source held a valid 350-file/2,938-chunk cache while the durable location held an older 345-file/2,855-chunk cache.
- Two stale R01 repositories, three ignored G-02 files, 47 local branches, 68 remote branches, and stale local exclude/tracking entries remained outside the tracked cleanup diff.
- The first measured research closeout reported roughly 5,580 seconds for each parallel agent. That value is the controller-observed dispatch-to-recording window, not proven agent active time. Token, tool, queue, and external-wait attribution were unavailable.

## Anomalies detected

### A1. Release review was advisory rather than fail-closed — HIGH

**Evidence:** PR #131 had no review; PR #132 had an unresolved Codex finding; repository rules required neither exact-head Codex evidence nor thread resolution.

**Interpretation:** Narrative release policy could be claimed after merge because GitHub did not enforce the same facts.

### A2. Installed OpenGSD instructions drifted from the active Codex schema — HIGH

**Evidence:** Generated adapters required `fork_context` and omission of inline model while the active typed spawn schema accepts explicit model, effort, and `fork_turns`. Agent TOMLs did not provide the missing binding.

**Interpretation:** Resolved Sol routing could silently inherit the global Terra default. Editing generated adapters would create an upgrade-fragile local fork, so the project must bind the live schema explicitly and fail closed where the vendor route is incompatible.

### A3. Mutating-agent authority contradicted repository isolation — HIGH

**Evidence:** `gsd-code-fixer` had a read-only sandbox and an unconditional worktree protocol; project configuration injected no policy into fixer or debugger.

**Interpretation:** The route could only fail late after spending context, or attempt a forbidden workspace topology. It must remain unavailable until both upstream constraints are corrected.

### A4. Completion checked tracked files but omitted external state — HIGH

**Evidence:** stale repositories, open superseded PRs, local/remote branches, Lumen config, global skills, and unresolved GitHub review state remained after the repository cleanup merged.

**Interpretation:** The prior completion boundary ended at the Git diff rather than the effective project system.

### A5. Observability mislabeled receipt delay as execution time — HIGH

**Evidence:** completion events were written when the controller processed results, and the summary named the resulting interval `wall`. The writer and summarizer also resolved `--repo` differently, dropped some event I/O, redacted only by key name, accepted inconsistent model keys, and silently treated unknown budget classes as `standard`.

**Interpretation:** The measurement layer produced precise-looking but semantically false cost data, obscuring rather than explaining the repeated delays.

### A6. Legacy retention had no terminal classification — MEDIUM

**Evidence:** unique and main-reachable branch tips, dirty and clean stale repositories, active open-PR heads, and obsolete global skills were all retained under different ad-hoc mechanisms.

**Interpretation:** Recoverability was used as an indefinite substitute for archive/delete decisions.

## Root cause

The repair was evaluated component-by-component instead of against one end-to-end system boundary. Repository code, installed OpenGSD behavior, GitHub enforcement, global semantic-search storage, and local/remote legacy state each had separate notions of completion. Advisory policy was mistaken for enforcement, upstream-generated instructions were trusted over the live runtime schema, and passive measurement used controller receipt time as agent execution time. The result was another premature completion claim.

## Global constraints

1. Native OpenGSD remains the sole project lifecycle. This repair adds no planner, retry loop, lifecycle state, graph, receipt, fingerprint, or second validator.
2. R01 remains paused. No product source or product test is in scope.
3. All repository work occurs in the primary project folder on a normal branch; no task may create or invoke a Git worktree.
4. Generated OpenGSD adapters and agent TOMLs are not hand-edited. Use an official pinned upgrade, supported project configuration, explicit active-schema binding, and fail-closed route isolation.
5. GitHub native rules enforce every fact they can express. The four CI status mappings are the owner-approved narrow exception to the former custom-code-only-for-Codex boundary. The only custom status surface is five independent, merge-resident contexts: `Metronome CI test-merge`, `Metronome XO test-merge`, `Metronome debt test-merge`, `Metronome observability test-merge`, and `Metronome Codex test-merge review`. Each is published only on the validated current open PR's `merge_commit_sha`; the first four map only their own platform-controlled job result, and the fifth maps only authenticated Codex evidence. No context may aggregate, query, summarize, or substitute for another gate. Protection requires exactly those five contexts from GitHub Actions App ID `15368`, strict up-to-date checks, resolved review threads, and no ordinary bypass.
6. Observability remains passive. Metrics that the host does not expose remain unavailable; they are never inferred as zero or used to authorize lifecycle state.
7. New or changed behavior follows RED -> GREEN -> REFACTOR. Every task records the focused commands and outputs.
8. Each implementation task must reduce or minimize maintained surface. No new dependency is allowed. Combined observability production LOC must decrease from the authorized baseline.
9. Every destructive cleanup first proves the exact target and recoverability classification. Unique history is archived and verified before ref deletion; main-reachable history is deleted without redundant archival copies.
10. Every new implementation and review subagent uses `gpt-5.6-sol`, `ultra`, and `fork_turns: none`.

## Task 1 — Repair and slim observability with TDD

**Files:** `scripts/gsd-observability-write.mjs`, `scripts/gsd-observability-summarize.mjs`, `tests/unit/gsd-observability.test.ts`, and only directly required package/runtime documentation.

1. Add focused failing tests for canonical repository-root resolution, all-event declared I/O aggregation, canonical `model` metadata, explicit unknown/unavailable token data, rejection of unknown budget classes, and correct naming of the controller-observed window.
2. Replace generic recursive metadata with a small explicit schema. Paths remain project-relative; timing values are finite non-negative numbers; unsupported/secret-shaped metadata is rejected rather than persisted.
3. Summaries must distinguish `observed_window_ms` from optional host-reported active/tool/queue/external-wait metrics. A soft budget may classify the observed window, but output must not call it agent active time.
4. Preserve sequential attempts, in-progress measurement, declared-only attribution, ignored `.logs`, compact output, and Windows stdin support.
5. Remove duplication and dead compatibility code. The combined writer/summarizer production LOC must be lower than before this task.
6. Record RED and GREEN focused test evidence, then run the complete observability unit file once.

## Task 2 — Publish five independent test-merge required contexts with TDD

**Files:** one narrow shared test-merge identity/status mapper and focused tests, the existing Codex evaluator/tests, the Codex workflow, the four existing CI/XO/debt/observability workflows, and this forensic contract.

1. Start with failing semantic tests for the exact five-name allowlist, prohibition on head publication, full PR/test-merge identity, pending-first behavior, terminal identity rereads and drift rejection, success/failure/cancellation/skipped mapping, workflow privilege separation, preserved commands/push behavior, pinned actions, absence of `merge_group`, and prohibition on cross-gate aggregation.
2. The narrow mapper accepts only an open, non-draft, mergeable, same-repository PR targeting `main`, with full distinct head/base/test-merge SHAs. It publishes only one allowlisted context to the validated `merge_commit_sha`, emits the complete initial identity, rereads that identity before a terminal write, and never reads another gate's status or check.
3. Convert the four normal-PR workflows to default-branch-controlled definitions. A write job checks out only the explicit default branch and runs the trusted mapper with minimum read plus `statuses: write`; a test job has only `contents: read`, no injected repository/environment/user secret, `persist-credentials: false`, and checks out the exact initialized test-merge SHA.
4. Preserve the existing CI lint/typecheck/unit/build commands, XO changed-file command, Semgrep debt validation commands, Windows repository-local npm wrapper, and existing CI/observability push-to-`main` execution. Push runs publish no PR test-merge status. Each terminal reporter consumes only its own platform-controlled job result.
5. Preserve the Codex evaluator's exact `chatgpt-codex-connector[bot]` login and REST user ID `199175422`, finding-free/current-head evidence format, latest-artifact ordering, force-push/base/request barriers, pending-first behavior, complete terminal identity reread, approved event surface, and default-branch-only execution. Any newer finding, missing environment, service error, unknown completion, identity drift, or reporter failure remains non-successful.
6. Add no dependency, lifecycle state, retry, cache, receipt, compatibility layer, merge queue, required-workflow abstraction, or aggregate controller. No context may aggregate, query, summarize, or substitute for another gate.

## Task 3 — Repair supported OpenGSD project binding and isolate fixer

**Files:** `.planning/config.json`, `AGENTS.md`, `skills/metronome-policy/SKILL.md`, and the existing executable gate/self-test files needed to lock the contract.

1. First add a failing executable contract proving that `gsd-code-fixer` and `gsd-debugger` receive `skills/metronome-policy`.
2. Lock the active typed dispatch contract to explicit native-resolved `agent_type`, `model`, `reasoning_effort`, and `fork_turns: "none"`; do not rely on inherited parent context or missing TOML fields.
3. Keep read-only code review available but prohibit fixer/auto-fix routing. Findings return to the normal native executor boundary. Fixer remains isolated until its installed sandbox is writable and its upstream prompt respects `workflow.use_worktrees=false`.
4. Debugger may operate only through its workspace-write sandbox in the primary project folder and with project policy loaded.
5. Do not edit or copy generated OpenGSD files into the repository. The official 1.8.0 upgrade and live runtime diagnostic occur only in Task 6.

## Task 4 — Review, verify, and merge the repository repair

1. After each task, generate a bounded diff package and obtain an independent 5.6 Sol Ultra spec/quality review; fix every Critical or Important finding and re-review.
2. Run focused tests, full lint, typecheck, unit tests, build, architecture/debt gates, and the relevant workflow checks on the final candidate.
3. Obtain a broad 5.6 Sol Ultra whole-branch review, then create the PR.
4. Because `pull_request_target` loads its workflow definition from the default branch, this bootstrap PR cannot self-prove contexts that are not merge-resident yet. Require the pre-existing exact-head CI evidence plus a finding-free read-only Codex review for the exact PR head, merge only that candidate, update local `main`, and prove a clean synchronized checkout. Do not claim the five new contexts until Task 5 proves them on a later PR.

## Task 5 — Enforce GitHub governance and remove legacy state

1. Publish and protect `codex/archive-legacy-overlay-review-7faa689f`; reply to and resolve PR #132's remaining thread without claiming retroactive gate success.
2. Record PR #131's historical review failure truthfully.
3. After the new workflow definitions are merge-resident, open a no-code bootstrap PR and prove `Metronome CI test-merge`, `Metronome XO test-merge`, `Metronome debt test-merge`, `Metronome observability test-merge`, and `Metronome Codex test-merge review` on its validated test merge. Then update `Protect Main` to require exactly those five independent contexts, each pinned to GitHub Actions App ID `15368`, plus resolved review threads, strict up-to-date checks, squash merges, and no ordinary bypass.
4. Close PRs #112, #113, and #129 as superseded. Recheck their head SHAs immediately before deleting branches.
5. Create and verify one bundle for the 16 unique local branch tips and preserve the dirty stale-repository state. Then delete all non-retained local/remote branches, stale tracking refs, obsolete G-02 files, stale exclude entry, old temporary repositories, and already archived global skill copies.

## Task 6 — Upgrade OpenGSD and migrate Lumen in one restart window

1. Install official pinned OpenGSD 1.8.0 without hand-patching generated files; back up relevant global config first.
2. Verify version, project skill resolution, native model resolution, installed sandbox assignments, and one read-only typed diagnostic spawn using explicit Sol/effort/`fork_turns:none`. Keep fixer isolated when its upstream incompatibilities remain.
3. Stop active Lumen processes once, verify the source database is checkpointed, copy the 350-file/2,938-chunk cache into same-volume staging under the durable data root, verify SHA-256/SQLite metadata/integrity, and atomically promote it.
4. Change only the global Lumen data-root setting, restart once, and prove health/status plus semantic-search reuse without forced reindex.
5. Retain one bounded rollback copy through final verification; remove the active experimental-path dependency.

## Task 7 — Independent final zero-debt audit

Dispatch a fresh 5.6 Sol Ultra agent with no inherited context. It must inspect repository/main cleanliness, effective OpenGSD routing and isolated routes, GitHub rules/reviews/checks/threads, Lumen durable storage, local and remote refs, stale repositories/files/skills, observability semantics, and the truthfulness of this report. Any remaining actionable debt returns to the appropriate task; no R01 work begins.

## Authoritative outcome

This section is available only after Task 7 returns zero actionable debt. The currently owner-authorized workflow-debt closure continues through Task 7; this section neither interrupts nor supersedes that closure, and it does not claim that Task 7 or any release fact is complete.

After that prerequisite, `$gsd-resume-work` may present exactly the three canonical choices from `.planning/forensics/report-20260722-124111.md:17-18`:

1. Abandon R01.
2. Redesign R01.
3. Start a different native milestone/phase.

Presenting these choices grants no product authorization. R01 remains paused until the project owner explicitly selects one choice and approves its boundary. Generic `continue`, `go`, `next`, or `resume` wording is not that approval.
