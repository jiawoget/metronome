# Forensic Report — local Semgrep prerequisite failure and bounded resolution

**Generated:** 2026-07-25T11:37:37+08:00

**Resolved:** 2026-07-25 after one owner-approved native repair and one fresh read-only re-review

**Problem:** The native `gsd-executor` completed the focused Task 2 re-review fix, but the mandatory pre-commit hook stopped at `validate:debt-gates` because `semgrep` was not resolvable. The investigation had to explain the late block, the earlier long waits, and whether removing Ponytail or Superpower removed Semgrep.

---

## Evidence Summary

### Git Activity

- **Last commit:** `fd72fdeea084bac6f4286b9aa559bd32d8e57041` at 2026-07-25 10:52:21 +08:00 — `fix(workflow): harden test-merge review gates`.
- **Recent history inspected:** 30 commits. The Codex evaluator and its focused test recur across several consecutive review/fix commits, including three consecutive commits on 2026-07-23.
- **Uncommitted state:** only `scripts/evaluate-codex-exact-head-review.mjs` and `tests/unit/codex-exact-head-review.test.ts` are staged; `.planning/STATE.md` is the only unstaged file.
- **Active worktrees:** one, the primary checkout. No product worktree or detached execution directory is registered.
- **Task 2 evidence before the block:** RED 111/114, GREEN 117/117, focused XO passed, full ESLint passed, typecheck passed, and maintained evaluator LOC decreased from 214 to 209 physical lines. No commit was created because the hook failed closed.

### Planning State

- **Milestone:** v1.1 — R01 Evidence-First Code Slimming.
- **Current phase:** 01 — canonical-practice-presentation-formatting.
- **Status:** paused; R01 product execution remains unauthorized.
- **Authorized work:** only the workflow-debt closure in `report-20260722-214242.md`.
- **Current blocked step:** `task-2-native-rereview-fix`; the compact observability summary records about 19.6 minutes from dispatch to blocked recording. Host active/tool/queue time and tokens are unavailable.

### Artifact Completeness

| Scope | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|---|---:|---:|---:|---:|---:|
| Active Phase 01 | none active | none active | none active | none active | none active |
| Frozen failed attempt | quarantined | not lifecycle-active | quarantined | quarantined | quarantined |

The absence of active phase artifacts is intentional: the product phase is paused and the failed attempt is quarantined under `.planning/forensics/`. It is not a missing-artifact anomaly.

### Semgrep Resolution

- `Get-Command semgrep`, `where semgrep`, `python`, and `py` all failed in the current process environment.
- The user-level PATH contains only WindowsApps; neither the user PATH nor current process PATH contains Miniforge.
- Semgrep was **not removed**. `%USERPROFILE%/miniforge3/Scripts/semgrep.exe` exists, and the matching Python environment reports Semgrep `1.168.0` installed in `%USERPROFILE%/miniforge3/Lib/site-packages`.
- The exact executable returned `1.168.0` in about four seconds under one elevated probe.
- Historical installation evidence also points to `%USERPROFILE%/miniforge3`, not Ponytail or Superpower. The original install attempts downloaded Semgrep `1.168.0` into that Python environment.
- No active tracked repository reference to Ponytail or Superpower exists. The inactive Ponytail cache contains no Semgrep executable. There is no evidence that either plugin owned or deleted Semgrep.

### Process Evidence

- `scripts/run-metronome-semgrep-changed.mjs` calls `semgrep --version` through synchronous `spawnSync` with no timeout before every scan.
- An exact sandboxed `semgrep --version` probe failed to return after more than 60 seconds.
- Process inspection found one abandoned `osemgrep -> pysemgrep -> python` version-probe chain from 09:54 and one chain created by the 11:33 forensic reproduction.
- Both verified Semgrep version-probe chains were terminated. Other Python services were left untouched. A post-cleanup check found no remaining Semgrep/pysemgrep version probe.
- The same exact command succeeds when elevated, so the sandbox hang and PATH resolution failure are distinct conditions.

### Local versus CI Contract

- CI explicitly runs `python -m pip install semgrep` before the debt gate.
- The local hook has a repository-local Node/npm fallback, but it neither provisions nor resolves Semgrep.
- The runner's error says to install Semgrep even when the tool is already installed but absent from PATH.
- No repository-local Semgrep version lock or local bootstrap contract exists.

## Anomalies Detected

### A1. Ambient external-tool dependency — Confidence HIGH

**Evidence:** CI installs Semgrep, while the local hook assumes a bare `semgrep` command. The executable remains installed outside PATH.

**Interpretation:** Local gate success depended on whichever shell or agent happened to expose Miniforge. Past success was not durable evidence for a later process environment.

### A2. A resolved executable entered an unbounded availability probe — Confidence HIGH

**Evidence:** `hasCommand()` synchronously runs `semgrep --version` without a timeout. A missing bare command returns `ENOENT` immediately; the long-running sandbox reproduction and the 09:54 process chain occurred only after an executable had been resolved and started.

**Interpretation:** The workflow can spend minutes or hours before scanning any file. Retrying or interrupting the parent does not reliably clean the full Windows child tree.

### A3. Prerequisite validation happened after implementation — Confidence HIGH

**Evidence:** The executor reached RED, GREEN, refactor, XO, ESLint, and typecheck before discovering that the mandatory commit gate could not resolve Semgrep. The blocked observability window is about 19.6 minutes.

**Interpretation:** The task paid nearly its full implementation cost before checking one mandatory external executable. This is the immediate reason Task 2 did not advance despite correct focused code.

### A4. Missing-tool and missing-PATH states are conflated — Confidence HIGH

**Evidence:** The runner emitted `Missing semgrep. Install ...`; direct inspection proves Semgrep 1.168.0 is installed.

**Interpretation:** The diagnostic steers agents toward reinstall attempts, recreating the earlier timeout loop instead of resolving the executable deterministically.

### A5. Repeated-edit loop signal — Confidence MEDIUM

**Evidence:** The evaluator/test pair appears in multiple review/fix commits, including three consecutive commits, and the current uncommitted fix follows two more Task 2 commits.

**Interpretation:** Independent review is finding real edge cases, but late environment checks add a separate loop after the code loop has converged.

## Other Anomaly Checks

- **Missing artifacts:** not detected; no active product plan exists by design.
- **Partial-plan drift:** not detected for product work; the staged Task 2 repair is intentionally blocked before commit and is fully identified.
- **Abandoned work/crash:** no orphan worktree and no unexplained source drift. Confirmed Semgrep probe processes were cleaned.
- **Scope drift:** not detected; staged code remains inside the approved Task 2 evaluator/test boundary.
- **Test regression:** not established. Focused tests pass; the full unit command produced no terminal result envelope, so it remains unclaimed and must run once through the repaired hook.
- **Legacy control flow:** no active Ponytail/Superpower repository authority or process was found. Their removal did not require a repository migration step; native OpenGSD remains the sole lifecycle.

## Root Cause

The local debt gate treats Semgrep as an ambient, late-bound executable instead of an explicit external prerequisite. Native OpenGSD correctly stopped at the mandatory hook, but the task did not verify that prerequisite before dispatch. Two separate outcomes were previously conflated: a bare command absent from PATH fails immediately with `ENOENT`, while an explicitly resolved Semgrep executable can start and then hang inside the redundant sandboxed `--version` probe. The latter can leak child processes before any scan begins.

Ponytail and Superpower are not the root cause supported by the evidence. The known Semgrep installation remains intact in Miniforge; only its executable resolution is absent from the current user/process PATH.

## Recommended Repair Boundary

The complete repair is one bounded workflow prerequisite fix; it does not authorize R01:

1. Reuse the installed official Semgrep `1.168.0`; do not reinstall it and do not add another scanner.
2. Keep executable selection explicit through the runner's existing `SEMGREP_BIN` contract. Do not broaden the user PATH. Bind the exact installed executable for local elevated gate execution.
3. Through native `gsd-executor`, remove the redundant `hasCommand() -> semgrep --version` probe. Invoke the actual scan once; report `ENOENT` immediately as executable-resolution failure, preserve real scan exit status, and add a focused black-box regression proving that a successful invocation receives `scan` directly and is never called with `--version`.
4. Complete the already-staged Task 2 repair and the bounded runner/selftest correction as one reviewed candidate before creating a commit. Do not stage `.planning/STATE.md` or this report in the executor commit.
5. Let one elevated, non-bypassed full hook provide the authoritative Semgrep, full-unit, and build result. If the missing terminal envelope repeats, record that as a new forensic fact instead of silently rerunning.
6. Obtain a fresh finding-free native read-only `gsd-code-reviewer` review over the complete Task 2 plus prerequisite-fix range before Task 4 resumes.

### Owner decision

On 2026-07-25 the owner required the corrected fix after distinguishing immediate executable-resolution failure from a resolved executable that hangs. This authorizes the exact six-step boundary above. It does not authorize PATH mutation, reinstall, retry machinery, a process manager, another scanner, or R01 product work.

## Resolution Evidence

All six authorized repair steps are complete:

1. The existing Semgrep `1.168.0` installation was reused. No install, alternate scanner, or PATH mutation occurred. The durable local selector is the user-level `SEMGREP_BIN` value pointing at `%USERPROFILE%/miniforge3/Scripts/semgrep.exe`; the already-running Codex host supplied that exact value inline because it cannot inherit a newly written user environment value.
2. Native `gsd-executor` removed the redundant `--version` availability probe. The production runner now invokes the selected executable once with `scan`; spawn `ENOENT` remains an immediate, truthful resolution failure and nonzero scan status remains a scan failure.
3. The regression selftest uses a uniquely identified fake executable boundary and records every complete `command + args` invocation without filtering. Its calibration proves that an unexpected `--help` call is visible; the actual runner assertion then requires exactly one invocation whose first argument is `scan`.
4. The final implementation commit is `5b6c77edf1886288ac5619d03dcc105c8b409ac3`. It contains exactly the four Task 2 files, keeps the production runner byte-identical to the pre-selftest-amend candidate, and preserves the measured production reduction of eight physical lines.
5. Exactly one elevated, non-bypassed amend hook ran after the test-contract correction. It completed in about 75.53 seconds: debt selftest passed; changed-file Semgrep ran 22 rules with zero findings; changed XO, full ESLint, typecheck, all 70 unit-test files and 1,065 tests, and the production build all passed.
6. A fresh native `gsd-code-reviewer` bound to `gpt-5.6-sol`, `xhigh`, and no inherited parent context reviewed exact HEAD `5b6c77edf1886288ac5619d03dcc105c8b409ac3` over `5058b239..5b6c77e`. It returned no blocker, warning, or other actionable finding; the prior Semgrep test-contract blocker is closed, and both spec compliance and maintainability passed.

The repair did not dispatch product work, resume R01, create a worktree, push, or change lifecycle authority. Task 2 may now advance only through the separately authorized workflow-debt release path.

---

*Report generated by `$gsd-forensics`. Absolute user paths are redacted. No product source was modified.*
